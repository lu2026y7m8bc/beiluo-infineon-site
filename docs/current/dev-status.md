# dev-status.md — BeiLuo Infineon Site

> Last updated: 2026-07-05 (T9.1/T9.2 completed (see prior entries). User asked to resume T8.1/T8.2's remaining smaller findings, tiered by scope: **Tier 1 (data/template wiring + title/CTA fixes, no new content) done, Codex-approved and committed (c6cbcae)** — `hero.bgSvgAlt`/`solutionsTeaser.industry`/`supportTeaser.category+date` wired up, 3 title-keyword gaps fixed, product-detail's CTA wording/action-count fixed, `products-list`'s CTA-vs-ghost-link mismatch fixed, `solutions.json`'s dead `blockDiagram` field removed. Zero overflow regression across all 52 pages × 12 widths, reviewer+Codex approved. Tier 4 (`sidebarSections` wiring), Tier 2 (new SVG/PDF assets), and Tier 3 (WeChat popup + Home Appliance solution) remain, in that priority order per user's own tier selection.)

---

## 1. Branch / Worktree

- Branch: `feat/beiluo-infineon-site`
- Base: `main`
- Worktree: single (no linked worktrees active)
- Working tree: **dirty** — T8.1/T8.2's "Tier 1" small-fixes batch (`src/data/home.json`/`home.schema.md`/`solutions.json`/`solutions.schema.md`, `src/lib/pages.js`, `src/templates/{home,product-detail,products-list}.html`, `tests/pages.test.js`) is complete and Codex-approved but **not yet committed**, pending user confirmation. Everything else (T9.2's batches 9675396/4bbe699, T9.1's 34ce7b3, T8.1's CSS batch 3b9abd3, all of T8.2's earlier work) is already committed.

---

## 2. Completed Tasks

| Task | Description | Commit(s) |
|------|-------------|-----------|
| T0.1 | Project scaffold | 5466df4 |
| T0.2 | Test runner (npm test) | — (covered by T0.1) |
| T0.3 | JSON schema drafts | 5c0d2a8 + fix a479f7f |
| T0.4 | Markup contract | 76c643b + fix f11bce0 |
| T0.5 | check_list1.md | 5688aae + fix e07b3f9 |
| T0.6 | check_list2.md | a3ddd7d |
| T1.1 | slugify.js | 700d694 |
| T1.2 | render.js | 78caafd + d579ca2 |
| T1.3 | schema.js (7 JSON-LD types) | 48de8ba + fbc5ee3 + 14f3623 |
| T1.4 | links.js | a9cce0c + 671447c |
| T1.5 | sitemap.js + robots.js | 8820d59 |
| T1.6 | pages.js | 31d1410 + 0cf9460 |
| T1.7 | build.js | eb1bc20 + b90ccc0 |
| T2.1 | tokens.css | ccec85b + 667929d |
| T2.2 | style.css baseline | 00c5f79 |
| T2.3 | head-meta.html partial | f7d235c |
| T2.4 | nav.html + nav.js | 1f96354 + f4a57fc + 52c5964 |
| T2.5 | footer.html | 3d2f178 + 346f0fc |
| T2.6 | breadcrumb.html | b666ab9 + dbdb1ff |
| T2.7 | sidebar / contact-float / cta-block / card partials | b773dba + 099e66d |
| T3.1 | logo.svg + logo-mark.svg | dad45b3 |
| T3.2 | 4 category SVG icons | 6192875 |
| T3.3–T3.5 | Background SVGs + illustrations + product covers | 09e0722 + b5a7000 + 64f7b7b |
| T4.1 | site.json | e17cdc8 + 9db4d78 |
| T4.2 | products.json (4 categories, 8 models) | 2ddc96d + c4673bb |
| T4.3 | solutions.json (5 solutions) | 4a51ce3 |
| T4.4 | support.json (4 articles, 2 authors) | b1b91d7 + cb72698 |
| T4.5 | news.json (4 articles) | e8bfe33 |
| T4.6 | home.json + about.json | 26425ca + cb72698 |
| T4.7 | validate-data.js + 52 tests | 8653e3d + e733c21 |
| T5.1 | home.html (7 sections + JSON-LD) | 353ea00 + a507123 + 6a380a2 |
| T5.2 | products-list.html | 98b4bd6 + 99cd02b |
| T5.3 | product-category.html | 2153780 + 0066bae + 7d6a530 (dynamic-column defect fixed later in T6.1, see below) |
| T5.4 | product-detail.html | 7a99552 + 8cb349f (Tab markup-contract deviation fixed later in T6.2, see below) |
| T5.5 | solutions-list.html + solution-detail.html | e469571 |
| T5.6 | support-list.html + support-card.html partial (overview/category/tag, 19 pages) | d9f6bce |
| T5.7 | tech-detail.html (author bar/sticky sidebar/longform typography/related articles/TechArticle JSON-LD, 4 articles) | 8d0f5c7 |
| T5.8 | news-list.html + news-detail.html + news-card.html partial + news.json body HTML fix (5 pages) | ea5b726 |
| T5.9 | about.html + author profile pages + about.json team[] data fix (3 pages) | ecefabf |
| T5.10 | contact.html (contact card + inquiry form markup hooks) | 4b5f1e0 |
| T6.1 | table-filter.js **+ fixed a real T5.3 defect** (product-category.html tbody was hardcoded, ignoring 6–8 dynamic columns per category) **+ markup-contract.md §1 alignment** | eafdec4 |
| T6.2 | tabs.js **+ fixed a real T5.4 markup-contract deviation** (product-detail.html's 5 tabs had no `data-tab`/`data-tabpanel`, wrong id scheme) | e551dfc |
| T6.3 | toc.js **+ fixed a Major scroll-highlight semantic bug** found in review (see §6) | 53c6c8f |
| T6.4 | form.js **+ fixed a real T2.2 CSS contract deviation** (`style.css` styled `.is-error`, but `markup-contract.md` §4 mandates JS write `.is-invalid` — unused selector renamed to match contract) **+ added missing `.spinner`/`.is-submitted` CSS** (contract assigns these to "CSS 负责" but T2.2 never added them) | 5b66e86 |
| T7.1 | **Audit-only, no code changes.** Confirmed via full-site build + programmatic scan of all 52 `dist/**/index.html` that every page already has a unique, keyword-bearing `<title>`/`<meta description>`/`<link canonical>` and exactly one `<h1>` — satisfied incrementally by `pages.js`'s per-page-type `seo` object construction across T1.6/T5.9/T5.10 (see those tasks' history). Zero duplicate titles, zero duplicate descriptions, zero pages falling back to `site.json`'s `defaultTitle`/`defaultDescription`. | d9e9ba8 |
| T7.2 | **Found and fixed 3 real defects while auditing G8 (7-type JSON-LD).** (1) `products-list.html`/`product-category.html`/`product-detail.html`/`solutions-list.html` had **zero** BreadcrumbList JSON-LD despite design.md §10 requiring it on every non-home template — fixed by wiring `context.breadcrumbJsonLd` in `pages.js` (via `schema.js`'s `breadcrumbList()`+`jsonLdScript()`, finally used for the first time since T1.3), which activates an existing-but-previously-dead `{{#if breadcrumbJsonLd}}` hook already sitting in `breadcrumb.html`. (2) `product-category.html` was missing the `ItemList` design.md §10 explicitly assigns it — added via `context.itemListJsonLd` (same `schema.js` functions). (3) 14 JSON-LD field interpolations across 4 templates used HTML-escaping double-brace `{{x}}` instead of raw triple-brace `{{{x}}}` — a latent data-corruption risk (script-tag content isn't HTML-entity-decoded by JSON-LD consumers) that hadn't yet triggered with the current dataset. Full post-fix scan: 103 JSON-LD blocks across 52 pages, 0 parse failures, BreadcrumbList=51 (every non-home page, exactly once), ItemList=4 (one per category, previously 0), no duplicates. | f32001c |
| T7.3 | **Audit confirmed sitemap.xml/robots.txt generation itself was already correctly wired** (T1.5/T1.7 — `build.js` already calls `buildSitemap()`/`buildRobots()`; `dist/sitemap.xml` had all 52 pages, absolute URLs, well-formed XML; `dist/robots.txt` had the correct 3 lines). **Found and fixed 1 real defect**: `build.js`'s dead-link validation (`findLinkIssues`) never included `sitemap.xml`/`robots.txt` themselves in its valid-path set, so `footer.html`'s links to both (present on every page) were false-flagged as dead site-wide — 104 false positives (52 pages × 2 links), reproduced exactly via `git stash` before/after comparison. Fixed with a 2-line addition in `assembleSite()` pushing placeholder entries for both paths into the link-check input before validation runs. Remaining dead-link count after fix: 25 (all pre-existing, real, T9.1-scope model-slug cross-reference mismatches — unrelated to this fix). | c373abd |
| T7.4 | **FAQ structure confirmed already satisfied, no code needed** — `product-detail.html`'s `<details>`/`<summary>` accordion reads `products.json`'s `model.faq` (8 models × 5 questions each, within the 3–5 PRD threshold); FAQPage JSON-LD deliberately not added since G8's 7-type list doesn't include it. **Found and fixed 1 real "authoritative statement" defect**: `contact.html` had 3 hardcoded occurrences of specific numeric commitments ("Monday–Friday, 9:00–18:00 (GMT+8)" business hours, "within 24 hours" response time) with no backing `site.json` field — unlike every other contact fact on the site, these had been sitting as free-floating hardcoded text since T5.10 (already flagged as a Low finding, left unresolved until this task). Fixed by adding `contact.businessHours`/`contact.responseTime` to `site.json` (+ `site.schema.md` docs) and switching all 3 template occurrences to reference them — output text is byte-for-byte identical (verified via `git stash` before/after diff on rendered `dist/contact/index.html`), only the source of truth changed from 3 independent hardcoded strings to 1 shared field. | f21a8be |
| T7.5 | **Audit-only, no code changes.** Full-site scan of all 172 `<img>` tags across 52 pages: 0 missing `alt`, 26 `alt=""` all correctly paired with `aria-hidden="true"` (decorative icons next to visible text labels — the WCAG-recommended pattern, not a defect), 146 descriptive keyword-bearing `alt` values (spot-checked 30+, zero generic placeholders like "image"/"icon"), 104 inline `<svg>` (all from `contact-float.html`'s WhatsApp/WeChat widget icons, all already `aria-hidden`). G11 already fully satisfied by existing per-template `alt`/`imageAlt` field wiring. **This closes Phase 7.** | (docs only, this commit) |

**Milestone: PHASE 5 COMPLETE (T5.1–T5.10).** All 12 templates (home, products-list, product-category, product-detail, solutions-list, solution-detail, support-list, tech-detail, news-list, news-detail, about, contact) are built, reviewed, and Codex-approved. `node src/build.js` now runs end-to-end for the first time — see §6 for what that surfaced.

**Milestone: PHASE 6 COMPLETE (T6.1–T6.4).** All 4 interactive JS modules (table-filter, tabs, toc, form) are built and Codex-approved.

**Recurring pattern this session (T6.1–T6.4):** each Phase-6 JS task started by checking whether its target template/CSS actually matched `markup-contract.md`. Three out of four times (T6.1→T5.3, T6.2→T5.4, T6.4→T2.2) it didn't, and the earlier work had to be fixed first — in all three cases the underlying defect was more than a naming nitpick (T5.3's tbody was silently dropping most of the dynamic column data; T5.4's Tab markup had no `data-tab`/`data-tabpanel` at all; T2.2's CSS styled a class name `form.js` per contract would never write, so the error-state red border would never have appeared). **Check contract compliance before writing any further interactive JS — don't assume earlier "Codex-approved" work actually matches the contract it's supposed to.**

---

## 3. Current In-Progress

**T8.2 — check_list2.md full-JSON content scan.** Same tiered pattern as T8.1: dispatched 4 parallel audit agents to independently verify every `- [ ]` item in `check_list2.md` (7 JSON files × D1–D7 dimensions, ~350 individual checkable items) against actual field values — not trusting `npm test`/`validate-data.js` passing, since that only covers structural D1/D5/D7, not D2 (pure English)/D3 (brand differentiation)/D4 (keyword embedding)/D6 (placeholder alignment, i.e. "is this field actually rendered anywhere"). Result: **35 real content/wiring defects found** (full breakdown in §6 "T8.2 Audit Findings"), mostly a different character from T8.1's findings — orphan data fields never consumed by any template (nav.items, footer.columns, contact.* in the floating widget, several home.json teaser fields), rather than missing CSS.

User decision: fix only the 3 most severe items now, record the rest (~32 findings) as candidates — same tiered pattern as T8.1.

**Fixed this session:**
1. **`support.json` article category distribution** — 4 articles were `guides×2, application-notes×2, troubleshooting×0, reviews×0`, violating the schema's "exactly 4, one per category" quota; the `/support/troubleshooting/` and `/support/reviews/` category pages rendered with zero articles. Fixed by honestly recategorizing 2 of the 4 articles rather than fabricating new content: `how-to-select-infineon-mcu` (guides→reviews, retitled/reframed as an FAE comparison review — the body's AURIX-vs-XMC-vs-PSoC comparison already suited a "review" framing) and `infineon-optimos-mosfet-overview` (application-notes→troubleshooting, retitled/reframed — the body already had a "BeiLuo Field Application Tips" section discussing recurring customer issues, a natural troubleshooting fit). Since recategorizing changes each article's URL (category segment), **every hardcoded reference to the 2 old URLs was found and updated** across `home.json`'s supportTeaser, `products.json`'s `selectionGuideHref`, and `support.json`'s own internalLinks/body inline links — confirmed zero residual old-URL strings anywhere in `src/data/`.
2. **`products.json`: 13 dangling `PartRef` cross-references** across all 8 models' `alternativeParts`/`companionParts` (extends the 3 previously known from T8.1's audit to 10 more) — e.g. `TC265DA`, `1ED020I12-F2` (referenced 3×), `IKW50N65EL5`, `BSC0901NS`, none of which exist as real models. Fixed by either removing the dangling entry (when a real sibling-model entry already existed in the same array) or replacing it with a reference to one of the 8 real models with a freshly written, logically-sound pairing rationale (e.g. `TLI4966G` as a motor-feedback-sensor companion for the two IGBTs and the XMC4700 MCU). Verified zero dangling references and zero self-references remain.
3. **`support.json` + `news.json` metaDescription length** — several exceeded the 155-char schema limit (up to 175 chars); all 8 articles across both files now ≤155 chars.

Verification: `npm test` 363/363 pass; independent reviewer + Codex both confirmed via fresh scripted audits (0 dangling PartRefs, 0 self-references, correct 1-per-category distribution, 0 over-length metaDescriptions, 0 residual old URLs). Reviewer caught 1 Minor (a leftover "this application note" phrase in the recategorized troubleshooting article's closing paragraph, inconsistent with its new framing) — fixed before Codex sign-off. **Committed as e95bc7f.**

**Second batch fixed this session (§6.A "orphan data fields" — pending commit):**
1. **Mega Menu data wiring (`navCategories`)** — `nav.html` already had `{{#each navCategories}}` markup, but `navCategories` was never assigned in `pages.js`, so the Mega Menu's category/model sub-links never rendered on any of the 52 pages. Fixed by computing it in `buildPageList()` (2 featured models per product category) and attaching it directly onto `nav.items`' `megaMenu:true` entry, since this template engine's `{{#each}}` has no parent-scope access (`{{../x}}` unsupported) — the data has to live on the item being iterated, not a sibling top-level field.
2. **`nav.items` data-driven rendering** — both the desktop nav and mobile drawer were 7 hardcoded `<li>` elements; both now loop `{{#each nav.items}}`, with the Products item's `has-megamenu` class/`data-megamenu-trigger` attribute/mega-menu panel rendered conditionally via `{{#if megaMenu}}`.
3. **`footer.columns`/`copyright`/`sitemapHref`/`robotsHref` wiring** — all 4 footer columns and the bottom-bar copyright/meta-links were hardcoded. `validate-data.js` hard-requires `footer.columns.length === 4`, so the brand column (index 0) couldn't be dropped from the data even though its flat "heading+links" shape doesn't fit the richer 3-line brand.name/slogan/oneLiner layout — kept index 0 hardcoded from `brand.*` (richer) and loop columns 1-3 via `{{#each footer.columns}}` + `{{#if @index}}` (0 is falsy, skips the brand column cleanly). Added an `"external": true` field to the WhatsApp link entry in `site.json` so the loop can conditionally add `target="_blank" rel="noopener noreferrer"` without hardcoding. One accepted micro-behavior change: the WeChat line was previously non-clickable `<span>` text, now a clickable link to `/contact/` (its `footer.columns` entry always had an `href`; not a regression, just newly wired up).
4. **`contact.whatsappHref` wiring** — `contact-float.html`'s WhatsApp button used a hardcoded `wa.me` URL; now reads `{{contact.whatsappHref}}`.
5. **`seo.siteName` → `og:site_name`** — added `{{#if seo.siteName}}<meta property="og:site_name" ...>{{/if}}` to `head-meta.html`.
6. **Organization JSON-LD completion** (`home.html`, hand-authored inline block, not schema.js's unused `organization()` helper) — added `logo` (ImageObject with an absolute URL), `foundingDate`, `areaServed`, `contactPoint`; changed `description` from `brand.oneLiner` to `brand.slogan` per `site.schema.md`'s documented field mapping. Confirmed this doesn't add a new JSON-LD **type** (still `Organization`), so the G8 "exactly 7 types" rule is untouched.
7. **Bonus fix, discovered while testing #5 above (not in the original findings list):** `og:site_name` rendered on 50 of 52 pages but was missing on `/` and `/about/`. Root cause: those 2 pages build their context via `{ ...site, ...home/about, breadcrumb }` (a full top-level object spread), and `home.json`/`about.json` each have their own flat `seo: {title, description, canonical}` object — since object spread does a shallow key-level overwrite, `...home`/`...about` silently replaced `site.seo` entirely, dropping `siteName`/`defaultTitle`/`defaultDescription`/`baseUrl`. This was latent before (harmless today only because both pages happen to always provide their own `seo.title`, so the `{{else}}` fallback to `seo.defaultTitle` was never exercised) but would have broken outright if either page's `seo.title` were ever empty. Fixed by explicitly constructing `const seo = { ...site.seo, ...home.seo }` (and same for `about`) before building the context. Reviewer additionally found and I hardened one more instance of the same latent risk: `support-list`'s context previously spread `...support` *after* its already-correct `seo` variable, meaning a future `support.json` top-level `seo` field would silently repeat this bug — reordered so `seo` comes after `...support`.

Verification: `npm test` 363/363 pass; independent reviewer confirmed via fresh scripted audit across all 52 pages (correct Mega Menu category/model data matching products.json, exactly 1 `data-megamenu-trigger` and 7 nav links per page, 4 footer columns per page with correct WhatsApp/WeChat/copyright/meta-link behavior, `og:site_name` present on all 52, Organization JSON-LD fields all correct, 0 new dead links via a git-stash-based before/after comparison) and manually re-read every `pages.js` page-construction block to confirm only home/about had the seo-merge bug. Reviewer's 1 Minor/Low finding (the `support-list` ordering risk) was fixed before Codex sign-off. Codex approved (its sandbox again couldn't rewrite `dist/`, ran the audit script against the already-built output per the established workaround).

**NOT done — do not assume complete:** ~28 smaller findings remain (§6.A's contact-float WeChat-QR-toggle has literally no JS handler — a bigger fix than data-wiring, deliberately deferred; solutions.json's dead `blockDiagram` field + under-quota `bomList` + wrong industry coverage; news.json's non-first-person company-article voice; and more — full list in §6, categories B/C/D). T8.2 should stay `in_progress` in `todo_write.md`.

**Third batch fixed this session (§6.B content-quality + §6.C count-quota — pending commit):**
1. **`home.json` content-quality (3 items)** — `hero.headline` reworded (turned out to be the exact literal example text from `home.schema.md`'s own field table, not real copy); `productsTeaser[sensors].summary` now opens with "Infineon Sensors" matching the other 3 categories' keyword pattern; `solutionsTeaser[industrial-automation].summary` gained a BeiLuo/FAE-context clause matching its sibling teasers.
2. **`news.json` voice/sourcing (3 items)** — both `"company"`-type articles (`beiluo-expands-infineon-igbt-stock-2024`, `beiluo-fae-support-program-launch`) fully rewritten from third-person to first-person corporate voice per `news.schema.md`'s explicit requirement, preserving every H2 id, internal link, and staying ≥800 words (996/1007). One `"industry"`-type article's unattributed "market analysts estimate..." claim now cites a real named source (Yole Group); independently re-verified that the *other* industry article doesn't have this problem (the original finding's "both" was an overstatement, corrected here).
3. **`solutions.json` count quotas (3 items, all 5 solutions touched)** — `bomList` expanded from 2 to 3 real (not fabricated) entries per solution; the `industry` duplication (`Industrial Automation` × 2) resolved by relabeling `mcu-embedded-control` to `Embedded Control` (matches its own title, no fabricated "Home Appliance" label — see below); `blockDiagram.alt`/`diagramAlt` expanded past the 40-word minimum on the 2 solutions that were short, kept in sync with each other.
4. **`about.json` count quota (1 item)** — `advantages` merged from 6 to 5 items (two speed-related entries combined, zero factual detail lost).

**User decision point:** the "Industrial Automation" duplication + missing "Home Appliance" finding required a judgment call — neither duplicated solution's real content honestly supports a "Home Appliance" relabel. Asked the user via `AskUserQuestion`; they chose to fix only the duplication (done) and leave the "Home Appliance" industry gap open rather than force a mismatched label or write an entirely new ~800-word solution in this pass. That gap is now its own tracked candidate (see §6.C), not silently dropped.

Verification: `npm test` 363/363 pass; independent reviewer re-verified every claim from scratch (word/pronoun counts on the rewritten news bodies, H2-id/internal-link preservation, a from-scratch re-read of the "AURIX article has no sourcing problem" claim, scripted checks that all 5 `bomList`s are length-3 with real `partNo`s, industries are unique, `diagramAlt`/`blockDiagram.alt` stay in sync and ≥40 words, `advantages` is 5 items with no lost detail) plus a git-stash dead-link before/after comparison (unchanged). Codex approved (same sandbox-EPERM workaround as prior rechecks this session).

**Fourth batch fixed this session (`products.json` orphan fields, §6.A carryover — pending commit):**
1. **`Product` JSON-LD completion** (`product-category.html` + `product-detail.html`) — both templates' hand-authored `Product` JSON-LD blocks gained `sku` (= `partNo`), `image`, and `category` (= `applications.0`, using `render.js`'s numeric dot-path array access — verified this actually works, not assumed) per `products.schema.md`'s documented field mapping (lines 168-182).
2. **`model.shortDescription` made visible** (`product-detail.html`) — added as a `<p class="product-hero__short-desc">` in the info bar per schema's "shown in right info bar... alongside stock badge" spec; no new CSS needed since sibling elements in that info bar (`__series`, `__package`, `__brand`) already have no dedicated styling either.
3. **`category.selectionGuideHref` wired** (`product-category.html`) — added a "Selection Guide →" link in the category hero. **Deliberately did NOT wire `selectionGuideDownloadHref`** ("Download Category Selection Guide" button): discovered `src/assets/docs/` doesn't exist on disk at all, so all 4 categories' PDF hrefs point to files that were never created. Rendering that button would add a control that always 404s — and `links.js` only validates internal *page* routes, not static asset files, so this specific class of dead link would never be caught by the build's own dead-link check. Left unwired; the missing PDFs are already tracked as their own asset-gap finding.
4. **Fixed 2 newly-exposed dangling references** (`products.json`) — wiring `selectionGuideHref` into a real, rendered link immediately surfaced that `mosfet` and `sensors` categories' values pointed to support articles that never existed (`/support/guides/how-to-select-infineon-mosfet/`, `/support/guides/how-to-select-infineon-sensors/`) — invisible dead data until now, since the field was never rendered before. Repointed both to the closest real, existing articles (`infineon-optimos-mosfet-overview`, `infineon-sensor-iot-applications`).
5. **`category.title` re-confirmed NOT an orphan field** — already correctly wired as the page's `<title>` tag via `pages.js` (`seo.title = category.title`); the original finding was about it not *also* driving the visible H1, which turns out to be normal, intentional SEO practice (title tags and H1s commonly differ), not a defect. No change made.

Verification: `npm test` 363/363 pass; independent reviewer scripted checks across all 4 category pages (Selection Guide links resolve to real support articles, no download button/PDF reference rendered) and all 8 detail pages (visible `shortDescription` present, `Product` JSON-LD valid with `sku`/`image`/`category` populated); independently confirmed `src/assets/docs/` doesn't exist and that `links.js` skips `/assets/` paths entirely (read the implementation, not just my claim); independently tested the `{{applications.0}}` array-index syntax against `render.js` directly. Reviewer found 1 Minor (the newly-added top-level `url` field in both JSON-LD blocks uses a relative path, where `products.schema.md` documents it should be absolute) — investigated fixing it, but `product-category.html`'s block sits inside `{{#each category.models}}` which has no parent-scope access to `jsonLd.organizationUrl`, so a full fix needs `pages.js` changes beyond this batch's scope; reverted a partial fix to `product-detail.html` alone (would have made the two templates *more* inconsistent with each other, not less) and left this as a tracked candidate instead. Codex approved (same sandbox-EPERM workaround).

**Fifth batch fixed this session (`Product` JSON-LD absolute URLs, follow-up on the fourth batch's deferred Minor — committed 590dcf0):** `products.schema.md` documents both `Product.url` and `Product.offers.url` as absolute. Fixed the parent-scope blocker properly this time (rather than reverting again): `pages.js`'s `categoryWithRowCells` construction now computes `absoluteHref: site.jsonLd.organizationUrl + model.href` on each model object itself — same pattern already used for the `navCategories`/Mega Menu fix earlier this session, since data that needs to be visible inside a `{{#each}}` loop has to live on the item being iterated, not a sibling top-level context field. `product-category.html` now uses `{{{absoluteHref}}}` for both `url` fields; `product-detail.html` (not inside a loop, so no blocker there) uses `{{{jsonLd.organizationUrl}}}{{{seo.canonical}}}` string concatenation for both. All 16 `Product` JSON-LD blocks across the site (8 on category pages, 8 on detail pages) now have absolute `url`/`offers.url`.

Verification: `npm test` 363/363 pass; independent reviewer wrote its own script confirming all 16 `Product` blocks' `url`/`offers.url` start with `https://www.beiluo.com` (0 relative), spot-checked that the path portion matches each product's real page path (not misconcatenated), and confirmed `site.json`'s `jsonLd.organizationUrl` value is what's actually referenced (not a hardcoded string). Reviewer noted 3 Low-priority, non-blocking items for future reference: `products.schema.md`'s own wording says "via site.seo.baseUrl" but the code (correctly, consistent with every other JSON-LD block sitewide) uses `jsonLd.organizationUrl` — a pre-existing doc-wording inconsistency, not a code defect; the `ItemList` JSON-LD's `ListItem.url` (a different, separate JSON-LD block, `pages.js` line ~155) is still relative — out of this fix's scope, a new candidate; no dedicated automated regression test asserts this specific absolute-URL invariant (existing `tests/schema.test.js` covers `schema.js`'s standalone helper functions, which these inline template blocks don't call) — verified only via the manual/script audit this session, worth a lightweight `dist`-scanning assertion in a future pass. Codex approved (same sandbox-EPERM workaround).

**Sixth batch fixed this session (regression test for the absolute-URL invariant, closing the fifth batch's own "no test coverage" candidate — pending commit):** added a test to `tests/pages.test.js` asserting that every model in a category page's context has an `absoluteHref` field equal to `site.jsonLd.organizationUrl + model.href` and starting with `https://`. Verified with a real red-green cycle: temporarily reverted `pages.js`'s `absoluteHref` computation to a plain relative `model.href`, confirmed the new test fails with the expected assertion error, restored the fix, confirmed it passes again (364/364).

Verification: independent reviewer redid the same red-green cycle independently (not trusting my report of it) and got the same result; confirmed the diff touches only `tests/pages.test.js` (no production code changes). Codex's first attempt returned a **spurious REJECT**: its own sandbox can't spawn child processes for `node --test` (the same `spawn EPERM` limitation already documented in the Fourth tooling note from T7.4), so its `npm test` invocation failed immediately with 9/9 files erroring before any test ran — Codex initially took this as a real failure. A same-task clarification citing 2 independent real-environment 364/364 pass results (controller + reviewer) plus the reviewer's red-green verification, along with a note not to re-run `npm test` itself, got Codex to instead read the diff directly and reverse to **APPROVE**. This is the exact T7.4 pattern recurring — logged as confirmation the established playbook still works, not as a new tooling note.

**Seventh batch fixed this session (`ItemList` JSON-LD absolute URL, the last of the fifth batch's 3 Low candidates — pending commit):** the fifth batch's reviewer noted `product-category.html`'s separate `ItemList` JSON-LD (listing all models in a category — distinct from the per-model `Product` blocks already fixed) also had a relative `itemListElement[].url`. Unlike the `Product` fix, this one required no `{{#each}}` workaround at all — `itemListJsonLd` is built entirely in plain JS in `pages.js` (`jsonLdScript(itemList(category.models.map(...)))`, no template loop involved), so the one-line fix was simply `url: m.href` → `url: site.jsonLd.organizationUrl + m.href`. Added a matching test to `tests/pages.test.js` that parses the `itemListJsonLd` script string and asserts every `itemListElement[].url` starts with `https://`; verified with a real red-green cycle (reverted, confirmed failure with the exact expected error message, restored, confirmed pass — 365/365).

Verification: independent reviewer redid the red-green cycle independently, confirmed the diff is exactly 1 production line + 1 test, and confirmed the fix doesn't affect the already-fixed `Product`/`BreadcrumbList` JSON-LD in the same page. Codex hit **two** rounds of friction, both non-code: round 1 was a genuine transient network outage (`codex doctor` showed both `websocket` and `reachability` failing, the same diagnostic signature as T7.5's outage — confirmed real via the diagnostic tool, not assumed) resolved by waiting and retrying; round 2 was a **spurious REJECT** citing `CLAUDE.md` appearing in `git diff --name-only` as evidence of "scope creep" — but that single-line `CLAUDE.md` change (a stale test-count comment) has been sitting uncommitted since before this session's work began and is unrelated to this fix, an almost exact repeat of the T7.4 false objection over the same two files. A same-task clarification citing that precedent plus a fresh `git clean -fdx dist/` (there were also unrelated untracked build artifacts inflating `git status`) got a clean **APPROVE** — Codex this time additionally ran `node tests/pages.test.js` directly (works fine as a single process, unlike `node --test`'s per-file child-process spawning) for its own 50/50-pass evidence and wrote its own independent script confirming 0 bad URLs across the real category pages.

---

**T8.1 — check_list1.md full-template scan — RESUMED, `in_progress` again.** Was paused after its 3 systemic fixes (see below) while the user worked through T8.2; the user then explicitly asked to resume T8.1's CSS work. `todo_write.md` updated: T8.1 back to `in_progress`, T8.2 back to `pending` (its own work is all committed, ~13 smaller findings remain as candidates, tracked separately).

What happened in that session: dispatched 4 parallel audit agents to independently verify every `- [ ]` item in `check_list1.md` (13 template/variant sections × D1–D9 dimensions, ~365 individual checkable items) against the actual rendered `dist/` output and template source — not just re-reading the templates, but running `node src/build.js` and grepping/parsing the real HTML. Result: **~150 items failed**, revealing 3 systemic root-cause defects plus a much larger, separate scope of missing page-specific CSS and assorted smaller gaps (full breakdown in §6 "T8.1 Audit Findings").

**Fixed this session** (user-approved tiered decision: fix the 3 systemic root causes now, record everything else as candidates, do not attempt the large CSS-writing effort in this pass):
1. `src/build.js` never copied `src/assets/**` (CSS/JS/SVG) to `dist/assets/` — the entire deployed site would have loaded with zero stylesheet, zero JS, zero images. Fixed with a new `copyAssets()` step using `fs.cp(..., {recursive:true})`.
2. `src/templates/solution-detail.html` rendered `solution.body` as raw unconverted Markdown (`##` headers visible as literal text, no `<p>`/`<h2>` tags) on all 5 solution pages — same class of defect T5.8 already fixed for `news.json`, but `solutions.json` was never converted. Fixed by hand-converting all 5 bodies to HTML (surgical string replacement, not a full JSON re-serialize, to avoid reformatting unrelated fields) and adding the `.longform` utility class (already existed in `style.css` since T2.2, just never applied) to `.solution-body`.
3. `src/lib/pages.js`: **10 separate page-construction blocks** built a `breadcrumb` array missing that page's own crumb level (e.g. `/contact/`'s visible breadcrumb showed only "Home", `/products/igbt/`'s showed only "Home / Products" with no category level) — causing the visible nav breadcrumb to have fewer levels than the same page's `BreadcrumbList` JSON-LD everywhere this was checked. Fixed all 10 (about, contact, products-list, product-category, solutions-list, solution-detail, support overview, support category index, support tag pages, news-list, news-detail); crumb names verified to exactly match each page's existing hand-authored JSON-LD (e.g. "About Us" not "About"). One test (`tests/pages.test.js`, previously asserting the buggy 1-level `/about/` breadcrumb as correct) updated to assert the fixed 2-level behavior.

Verification: `npm test` 363/363 pass; independent reviewer + Codex both ran full-site scans confirming 0 breadcrumb/JSON-LD depth mismatches across all 52 pages, 0 raw Markdown remaining, `dist/assets/` byte-identical to `src/assets/` (SHA-256 compared).

**NOT done at the time — the large page-specific CSS gap (§6.A) was deferred, then picked back up in this session (see below).**

**T8.1 CSS batch — page-specific CSS coverage gap, the single largest T8.1 finding (pending commit).** `src/assets/css/style.css` (was 1163 lines, only covering shared global components — nav/footer/card/badge/form-base/sidebar/contact-float/breadcrumb/longform) had **zero CSS rules** for the majority of every template's own page-specific layout/component classes — confirmed via direct `grep` during the original audit, re-confirmed again before starting this batch. Added **22 new numbered sections** (15 through 37, ~1530 lines) covering all 13 page types: shared `.layout-with-sidebar` (7 templates)/`.page-hero` (5 templates)/`.tab-container` state styling (2 templates), then page-specific sections for home (hero/trust-bar/why-choose/4 teasers/final-cta), products-list (category cards), product-category (cat-hero/fae-note/spec-table/cat-cta), product-detail (product-hero/tabs content/alt-parts/companion-parts/product-cta), solutions-list (solution cards), solution-detail (hero/diagram/advantages/bom-table/cta), support-list (support-card-grid), tech-detail (full two-column layout/author bar/sticky sidebar), news-list, news-detail (banner/share-bar/latest-news), about (hero/timeline/client-cases/customs/team), author-profile (author-hero), and contact (contact-grid/contact-card/contact-form). Every new rule reuses `tokens.css`'s existing design tokens (colors/fonts/spacing/radius/shadows/breakpoints) — no new hardcoded values introduced.

Also fixed the **3 confirmed class-name mismatches** the original audit flagged, plus 1 more found while reading every template in full this pass:
1. `.btn--outline` (used in 5 templates, 12 places) → renamed to `.btn--secondary`, which already existed in CSS with the correct design.md §4.6 "Secondary" (blue outline) styling — the template name was wrong, not the CSS.
2. `specs-table`/`specs-table__row` (`product-detail.html` only) → renamed to `spec-table`/`spec-table__row`, aligning with `product-category.html`'s already-correct usage and design.md §4.9's naming.
3. `stock-badge`/`stock-badge--{value}` (`product-category.html` + `product-detail.html`) — the existing `.badge--instock`/`.badge--rfq` CSS classes were dead (never referenced by any template) because they use all-lowercase "instock" while `products.json`'s real `model.stock` values are camelCase ("inStock"/"rfq") and the templates interpolate that value directly into the class name. Rather than touch the data/validation layer, added new `.stock-badge`/`.stock-badge--inStock`/`.stock-badge--rfq` rules matching the template's actual output, with the same visual design (verified the exact casing match against `products.json`'s real values, not assumed).
4. `.btn--primary` (`contact.html`'s form submit button, found while reading the template — not on the original 3-item list) → renamed to `.btn--cta`, the only class that's actually defined for the orange-CTA-with-dark-text style design.md §2.1 mandates; `.btn--primary` was never defined, so the site's own quote-request submit button had zero orange fill until this fix.

**Important limitation, stated explicitly per this project's own CLAUDE.md instruction to disclose when UI testing isn't possible:** this session's environment has no browser tool. Verification for this entire batch is **structural/spec-conformance only** — CSS syntax validity (brace balance), full HTML/CSS class-name cross-reference across all 52 rendered pages (confirming no leftover unstyled classes among genuine components, as opposed to elements correctly covered by global tag selectors or intentionally bare layout containers), the 4 class-name-mismatch fixes actually taking effect, and token usage vs. hardcoded values. **No one has visually looked at a rendered page in a browser.** If a genuine dev-server + browser check becomes possible, that should happen before this is considered fully done, not just structurally correct.

Verification: `npm test` 365/365 pass; independent reviewer independently rewrote (not reused) the HTML/CSS class cross-reference script across all 52 pages, found 55 "HTML class with no CSS rule" candidates, and individually judged each — ~53 were legitimate (h1-h6 elements covered by global tag selectors, `.section`-class containers needing no extra rule, implicit-grid layout parents whose children carry the styling, `.longform`-reusing content blocks, a native-`[hidden]`-driven tab-panel marker class) but found **1 Major** (`.card__tags`, used on 25 rendered support pages, had zero CSS — tag badges inside it would render visually glued together with no gap) and **1 Low** (`.solution-diagram__img` wasn't actually centered — the parent's `text-align:center` doesn't affect a block-level `<img>` under the sitewide `img{display:block}` reset). Both fixed (`.card__tags` gets `display:flex;flex-wrap:wrap;gap`; `.solution-diagram__img` gets `margin-inline:auto`), re-verified present in rendered HTML. Also separately independently verified the `.stock-badge--inStock`/`.stock-badge--rfq` casing exactly matches `products.json`'s real values (not assumed), and cross-checked design.md's token table against a sample of the new rules. Codex approved (its own audit script run against the sandbox's stale-but-controller-fresh `dist/`, same EPERM workaround as prior rechecks) and additionally spot-read the new CSS to confirm token (`var(--...)`) usage rather than hex/px hardcoding.

**T9.1 — full build + zero dead/empty link verification, in progress.** User switched from T8.1/T8.2 to Phase 9 integration verification once T8.1's CSS batch was committed. `node src/build.js` still reported the same 4 dead links noted since Phase 5 (all from `/` — home.json's `solutionsTeaser`/`supportTeaser`). Root cause confirmed: these 4 slugs/hrefs were fabricated placeholders from whenever `home.json` was originally authored, and never actually matched `solutions.json` (real slugs: `bldc-motor-drive`/`ev-charger-power`/`industrial-iot-sensor`/`mcu-embedded-control`/`solar-inverter-igbt`) or `support.json` (real slugs: `how-to-select-infineon-igbt`/`how-to-select-infineon-mcu`/`infineon-optimos-mosfet-overview`/`infineon-sensor-iot-applications`). Fixed by repointing all 4 to real content matched by industry/category theme (`motor-drive`→`bldc-motor-drive`, `ev-charging`→`ev-charger-power`, `industrial-automation`→`industrial-iot-sensor`, `infineon-coolmos-pfc-application`→`infineon-sensor-iot-applications` — this last one doesn't exist in `support.json` at all under any name, so it needed a genuinely different real article, not just a slug correction) and rewriting each teaser's `title`/`summary` as a faithful condensation of that real solution/article's own `summary` field — not new invented technical claims. Verification: `npm test` 365/365 pass; clean rebuild `exit 0` with no `[build] Link issues found:` output; `grep` for `href=""`/`href="#"` across all of `dist/` returns zero matches (also satisfies the "zero empty links" half of T9.1's criterion). Independent reviewer verified the diff touches only `home.json`, confirmed all 4 new slugs are real via direct cross-reference against `solutions.json`/`support.json`, and read the real vs. new-teaser summaries side by side confirming no fabricated technical details — APPROVE, with one non-blocking Minor suggestion (add a test asserting `home.json` teaser hrefs resolve to real slugs, to catch this class of bug automatically next time — not yet implemented). Codex approved (same sandbox EPERM-on-build workaround as prior rechecks; verified zero empty links via an equivalent read-only scan).

**T9.2 — real-browser UI verification, first time this session had an actual browser, in progress.** T9.2 requires responsive/interaction/AA-contrast/touch-target/reduced-motion testing that genuinely needs a browser — this session had none until now. `npx playwright install chromium` failed with a CDN geo-block (`403 Access denied... not available in your location` from `cdn.playwright.dev`'s Chrome-for-Testing path); `npx playwright install firefox` succeeded (Firefox's build lives under a different CDN path on the same host). Installed `playwright` as an ephemeral, unsaved dependency (`npm install --no-save`) — not added to `package.json`, doesn't affect the site's own zero-runtime-dependency architecture. Built a small zero-dependency static file server (Node `http` module) to serve `dist/` for the browser to hit.

Ran a comprehensive Playwright/Firefox suite across ~21 representative pages × up to 12 viewport widths (320–1440px) plus targeted interaction/contrast/motion checks. **Found and fixed real, previously-undetectable bugs** (T8.1's CSS batch had only ever been verified structurally):
1. **Nav overflow at exactly the 1200px desktop breakpoint** (~84px, hit nearly every page) — `.site-nav__links` is a `flex:1` child with no `min-width:0`, so its 7 nav items + logo + CTA's natural content width (1284px) refused to shrink below that regardless of viewport. Fixed: `min-width:0` on `.site-nav__links`, plus tightened `.site-nav__list` gap and `.site-nav__link` padding by one token step each (`--space-2`→`--space-1` gap, `--space-3`→`--space-2` padding) — closes the gap at exactly 1200px, verified empirically (not estimated from token math) by measuring real rendered widths at 1200/1240/1280px.
2. **Tablet (768–1199px) nav was never actually "condensed"** — design.md §7 explicitly specifies "横向（精简）" for tablet vs. "横向 + Mega" for desktop, but the CSS only had one `min-width:768px` breakpoint shared by both, so tablet showed the identical full desktop nav, overflowing on nearly every page in that range. Added a new `@media (min-width: 768px) and (max-width: 1199px)` block: hides the slogan, tightens link padding/gap further, shrinks font-size to caption size. Verified empirically at 768/900/1199px (the range's two ends plus a middle sample) — zero overflow.
3. **The mobile off-canvas nav drawer (`position:fixed` + `transform:translateX(100%)`) made the whole page horizontally scrollable in Firefox** — confirmed via `window.scrollTo(9999,0)` then checking `window.scrollX` (the real test for "can this page actually be scrolled", not just comparing `scrollWidth`/`clientWidth` which this specific case doesn't affect). Fixed with `html { overflow-x: hidden }` (didn't exist before) — note `overflow-x:hidden` on `body` alone is insufficient since the root scrolling element is `<html>`, not `<body>`, a distinction the fix had to get right on a second attempt after the first (`body`-only) attempt didn't actually stop `window.scrollX` from moving.
4. **Classic CSS Grid `min-width:auto` overflow bug, hit 3 separate T8.1-batch components for real** — grid items default to `min-width:auto`, which lets content's intrinsic width override its `1fr` share and blow out the whole page, rather than shrinking/wrapping as `1fr` implies. Found via browser at: `/about/`'s `.client-cases__card` (a company name too long to fit its column at 768px), `/products/`'s entire sidebar column (pushed off-viewport because `.layout-with-sidebar__main`'s own content forced the shared 2-column grid wider than intended), and `/support/guides/.../`'s `.related-articles` block (a *different*, more severe bug — see next item). Fixed with `min-width:0` on `.layout-with-sidebar__main`/`__sidebar`, `.tech-detail-layout > *`, plus a systemic defensive sweep adding `min-width:0` to every other T8.1-batch grid-item class sharing the same pattern (`.card`, `.card--teaser`, `.why-choose__card`, `.products-teaser__card`/`.solutions-teaser__card`, `.category-card`, `.alt-parts__card`/`.companion-parts__card`, `.solution-card`, `.team__card`, `.trust-bar__item`) even though only 2 of them had actually manifested within the tested breakpoint range — the same latent risk existed in all of them.
5. **`.related-articles` (tech-detail.html) was structurally misplaced in its own grid** — `.tech-detail-layout` defines a 2-column grid (`1fr 300px`: article content, sidebar) but has **3** top-level children (`.article-content`, `.related-articles`, `.sticky-sidebar`), and `.related-articles` — with no `grid-column` set — fell into implicit auto-placement, landing in the 300px sidebar column instead of spanning full width. Its `support-card-grid` of teaser cards, crammed into 300px, was what caused the earlier `.card--teaser`/`.card__image` overflow reports. Fixed with `grid-column: 1 / -1` on `.related-articles`.
6. **`.spec-table-wrap`/`.tab-list`/`[role=tablist]`/`.bom-table`** — added defensive `max-width:100%` (and `display:block;overflow-x:auto` for `.bom-table`, which had no scroll wrapper at all before). These were initially misidentified as overflow bugs by a naive per-element bounding-box check, but re-verified via the same `window.scrollX` test that the *page itself* never actually scrolls — these are properly-contained internal horizontal-scroll regions, exactly what design.md's own spec calls for ("产品分类表格...移动端横向滚动+首列冻结"). Kept the hardening since it's harmless and closes a latent risk, but logged this distinction clearly so it isn't miscounted as "bugs fixed" versus "defense added."
7. **3 real WCAG AA contrast failures**, measured via `getComputedStyle` + the actual WCAG relative-luminance/contrast-ratio formula (not eyeballed): `.stock-badge--inStock`/`.badge--rfq` (`--c-success`/`--c-warn` text on their own `-tint` backgrounds — 3.12:1 and 2.42:1 respectively, both below the 4.5:1 AA floor for normal text) and `.news-card__tag`/`.btn--secondary`-inside-`.product-cta` (`--c-primary` text on `--c-primary-tint` — 4.27:1, just under 4.5:1). Root cause: `--c-success`/`--c-warn` themselves don't clear 4.5:1 against their own tints (`--c-success` doesn't even clear it against plain white — 3.49:1), and `--c-primary` is fine everywhere it's normally used (on white) but not on its own tint. Fixed by adding two new tokens following the existing `--c-primary-dark` pattern — `--c-success-dark: #166B3A` (5.87:1 against the tint) and `--c-warn-dark: #8F4D00` (5.71:1) — and switching the 4 affected badge rules to them; `.btn--secondary` itself was left untouched (correct everywhere else it's used) with a scoped `.product-cta .btn--secondary` override instead.
8. **`.btn--sm` touch target was 36px, design.md requires ≥44px for all interactive elements with no stated exception** — bumped `min-height` to 44px (kept the compact padding/font-size, only touch-target height changed).
9. Verified via real click/fill/submit interaction, not code reading: tab switching (`aria-selected` and panel visibility toggle correctly), the table filter bar (JS-generated `<select>` controls present), TOC generation (8 anchor links generated), and the contact form (empty-submit shows 3 field errors, valid-submit shows the success message and hides errors — note the site's own JS has a real ~1000ms simulated-submit delay, which tripped up the first test pass with too short a wait, not a site bug).
10. `prefers-reduced-motion: reduce` confirmed working (pre-existing sitewide rule from T2.x, not touched this session) — `transitionDuration`/`animationDuration` both compute to `0s` under Playwright's `reducedMotion: 'reduce'` emulation.

Verification: `npm test` 365/365 pass; clean rebuild exit 0, 54 files, zero dead links. Independent reviewer **independently rewrote** the overflow-detection script (not reusing the session's), correctly identified and explained *why* checking `window.scrollX` after `scrollTo(9999,0)` is the right test versus a naive `scrollWidth`-vs-`clientWidth` per-element comparison (which would false-positive on the intentionally-scrollable table/tab containers), independently recomputed 2 of the 3 contrast fixes from real rendered colors via its own WCAG formula implementation, independently measured `.btn--sm`'s real rendered height, and independently drove the tab-click and form-submit interactions — catching and self-diagnosing two of its own test-script mistakes along the way (a mis-scoped form-error-count selector, and an initially-too-short wait for the form's real ~1s submit delay) rather than misreporting them as site bugs. APPROVE, one non-blocking Low note (design.md's own token table could document the 2 new `-dark` tokens, not required by this diff). Codex approved, confirming the same evidence via its own commands plus reading the diff directly to confirm every change traces to a specific browser-found issue with no unexplained speculative styling.

**T9.2 extension — same AA-contrast root cause found in 10 more instances, plus 1 unrelated one, after widening the test sample from ~21 to all 52 pages.** User asked to keep going on T9.2 rather than move to T9.3. Reused the same Playwright/Firefox setup, rebuilt a broader per-page/per-width overflow sweep (all 52 pages × 12 widths — zero real overflow, confirming the main batch's fixes hold at full coverage) and a broader contrast sweep (41 representative pages, all unique color pairs deduped). Found: `.badge--tag` and `.badge--category` (both widely used — support-card tags, tech-detail category label — same `--c-primary`-on-`--c-primary-tint` 4.27:1 shortfall as the main batch's fixes), `.solution-card__industry`/`.solution-hero__industry` (same root cause, on `/solutions/` and every solution-detail page), and the *same* pattern showing up in 6 `:hover`/`.is-active` state rules (`.btn--secondary:hover`, `.site-nav__link:hover`, `.mega-menu__model-link:hover`, `.site-nav__drawer-link:hover`, `.sidebar__link:hover`, `.sidebar__link.is-active`) — a plain grep for `color: var(--c-primary);` paired with `background: var(--c-primary-tint)` in the same rule turned up all of these at once rather than finding them one page at a time. All switched to `--c-primary-dark` (the same token used by the main batch). Separately found a **different root cause**: `.cta-block__text` uses `var(--c-footer-text)` (a muted gray-blue calibrated for the much darker `--c-ink-banner` footer background) against `.cta-block`'s own solid `--c-primary` background — only 3.29:1. Fixed with `var(--c-footer-heading)` (white, already used by the sibling `.cta-block__title` in the same component, not a new color choice) — 4.89:1.

Also confirmed (via screenshot, not just re-reading the DOM) that 2 remaining "failures" the checker still flags (`.news-banner__title`/`.news-banner__date`, both reading as white/light-gray on white) are a **structural limitation of ancestor-walk contrast checking, not a bug** — the dark gradient overlay that makes the actual rendered result readable lives on `.news-banner__overlay`, a sibling of the text content, not an ancestor, so a DOM-ancestor-walk can never see it. Visually confirmed correct via screenshot.

Verification: `npm test` 365/365 pass; clean rebuild exit 0; full 52-page × 12-width overflow re-sweep zero failures (no regression from the main batch); contrast re-sweep after the fix shows only the 2 known false positives remaining. Independent reviewer **independently reimplemented the WCAG contrast formula** (not reusing the session's), measured the fixed elements' real rendered colors directly (6.062:1 and 4.886:1, matching), and **used real mouse-hover actions** (not just reading CSS) to trigger 2 of the hover-state fixes and confirm their computed colors post-hover. Also spot-checked ~8 remaining un-fixed `color: var(--c-primary);` instances to confirm none of them pair with a `-tint` background (i.e., nothing was missed). Caught one accurate but non-blocking Minor: `.sidebar__link`'s fix is correct but currently unreachable in any real page, because `sidebarSections` (the data `{{> sidebar}}` needs) has never been wired in `pages.js` — a pre-existing gap already tracked in §6.B, not introduced by this fix, and not something "browsing all 52 pages" could literally have surfaced for that specific selector. APPROVE. Codex approved, confirming the diff is exactly the 10 `--c-primary`→`--c-primary-dark` swaps plus the one `.cta-block__text` token swap, no other properties or selectors touched. Committed (9675396).

**T9.2 closed out and marked `completed`.** Re-ran the `prefers-reduced-motion` check (Playwright's `reducedMotion: 'reduce'` emulation) across 6 representative pages (home, product-category, product-detail, about, solution-detail, tech-detail) — `transitionDuration`/`animationDuration` both `0s` on every sampled interactive element, on every page. This is a legitimate sufficient sample rather than needing all 52: the site has exactly one sitewide `@media (prefers-reduced-motion: reduce)` rule (a `*, *::before, *::after` universal selector, untouched this session), so per-page variation isn't possible by construction. All of T9.2's stated acceptance criteria are now met: zero real horizontal overflow (52/52 pages × 12 widths), zero real AA contrast failures, all touch targets ≥44px, tab/filter/TOC/form interactions verified via real interaction, reduced-motion confirmed. `todo_write.md` updated to `completed`.

**Standalone fix — "Top 8" ranking-wording consistency** (user-flagged, not one of T8.2's official 35 audit findings). The user noticed the site's "Top 8" market-position claim was worded inconsistently: `about.json` said "Top 8 Infineon distributor"/"Top 8 Infineon distributors in the Asia-Pacific region," `home.json`'s trust badges said "Top 8 Infineon Distributor," and `products-list.html` had a hardcoded "Top 8 infineon distributor in China" — three different claims (Infineon-specific ranking vs. general electronics-distributor ranking; China vs. Asia-Pacific scope). `docs/current/prd.md` line 8 and `docs/current/design.md` line 365 both specify the single canonical wording for the logo slogan: **"Top 8 Electronic Component Distributor in China"** — and `site.json`'s `brand.slogan`/`logo.alt` already matched this exactly, confirming site.json was correct and the other files needed to align to it, not vice versa. Fixed: `about.json`'s `seo.description`, `intro.body`, and the 2022 history milestone's `event`/`description`; `home.json`'s `hero.trustBadges[2].label` and `trustBar[2].label`; `products-list.html`'s hardcoded paragraph (also fixed a stray lowercase "infineon" while there). `about.json`'s `seo.description` edit specifically preserved the schema-required literal phrase `"Infineon authorized distributor"` (`about.schema.md` line 44) while still staying ≤155 chars (133 after edit). Verification: `npm test` 363/363 pass; independent reviewer confirmed zero old-wording residue anywhere in `src/`, verified the prd.md/design.md canonical source directly, confirmed the about.schema.md phrase+length constraint via script, and did a git-stash-based dead-link before/after comparison (identical 4-item list, no regression). Codex approved (first attempt's conclusion text was cut off by a 3-minute tool timeout after all verification commands had already succeeded with correct output; a follow-up request citing the same command output got a clean APPROVE).

---

## 4. Pending Tasks Overview

| Phase | Tasks | Status | Description |
|-------|-------|--------|-------------|
| Phase 6 (JS) | T6.1–T6.4 | **complete** | table-filter/tabs/toc/form — all Codex-approved |
| Phase 7 (SEO/GEO) | T7.1–T7.5 | **complete** | Meta wiring + JSON-LD + sitemap/robots + FAQ/GEO + image alt — all done |
| Phase 8 (Final sweep) | T8.1 pending, T8.2 pending | **paused** | check_list1 (3 systemic fixes + the large CSS gap both done, ~7 smaller findings remain) + check_list2 (all severe/orphan-field/content batches done, ~13 smaller findings remain) — user switched to T9.x, see §3/§6 for both |
| Phase 9 (Integration) | T9.1 completed, T9.2 completed, T9.3–T9.5 pending | **in progress** | T9.1 done (34ce7b3, zero dead/empty links). T9.2 done (4bbe699, 9675396) — real-browser testing across all 52 pages (Playwright+Firefox), zero overflow/contrast/touch-target issues remaining. T9.3-T9.5 (code review, PRD milestone verification, Codex full-product re-check) still pending |
| Phase 10 (Deploy) | T10.1–T10.3 | **BLOCKED** | Awaiting GitHub + Cloudflare credentials from user |
| Phase 11 (Wrap-up) | T11.1–T11.4 | pending | CLAUDE.md/dev-status final sync, branch finish, memory update |

Full task list with per-task completion criteria: `docs/current/todo_write.md`.

---

## 5. Last Test / Lint / Build Results

- **`npm test`**: **365/365 passing, 0 failures** (last run: T9.2 contrast-extension batch, 2026-07-05)
- **`node src/build.js`**: **exit 0, zero dead links, zero empty links**. 54 files written (52 pages + sitemap.xml + robots.txt).
- **Browser (T9.2, this session only — Playwright+Firefox, ephemeral `npm install --no-save`, not in `package.json`)**: zero real page-level horizontal overflow across **all 52 pages** × 12 breakpoints (320–1440px, verified via actual `window.scrollX` scroll-capability, not just `scrollWidth` — confirmed twice, before and after the contrast extension); 14 total AA contrast fixes verified ≥4.5:1 via real computed-style + WCAG formula (3 from the main batch, 11 from widening the sample to all 52 pages); touch targets ≥44px; tab/filter/TOC/form interactions verified via real click/fill/submit; `prefers-reduced-motion` confirmed working.
- **Lint**: no lint config in this project; ESM + Node.js built-ins only, no linter configured.

---

## 6. Known Issues (Not Yet Fixed)

### T8.1 Audit Findings (2026-07-05) — NEW TODO CANDIDATES, not yet implemented

4 parallel agents independently checked every `check_list1.md` item against real `dist/` output. The 3 systemic root causes (asset copying, solution-detail Markdown, breadcrumb depth) are fixed — see §3. Everything below is **recorded as a candidate for future work, per user decision, not yet implemented.** Scope/priority of the CSS work in particular needs a explicit decision before starting (it's large — see the estimate at the end).

**A. Page-specific CSS coverage gap (the largest item).** ~~`src/assets/css/style.css` (1163 lines) only styled shared global components... zero CSS rules existed for the majority of each template's own layout/component classes~~ — **fixed** (pending commit, see §3's "T8.1 CSS batch"): 22 new sections added covering all 13 page types + all 4 class-name mismatches resolved (`.btn--outline`→`.btn--secondary`, `specs-table`→`spec-table`, `stock-badge`→ new matching-casing rules, plus a 4th found while implementing: `.btn--primary`→`.btn--cta`). **Caveat: structural/spec-conformance verification only — no browser tool available this session, so no one has visually confirmed the rendered result.** `.layout-with-sidebar`'s sibling `sidebarSections` *data* gap (below) is a separate, still-open finding — the CSS now exists but the sidebar will still render empty until `pages.js` assigns `sidebarSections` somewhere.

**B. Data fields that exist but are never rendered:**
- `sidebarSections` is never assigned anywhere in `pages.js` — every page including `{{> sidebar}}` (products-list, solutions-list, solution-detail, support-list, news-list, product-category) renders a permanently-empty `<aside class="sidebar">`. This was already a known Low item (T5.x-era) but the T8.1 audit shows its blast radius is bigger than previously scoped — it affects the "sidebar navigation" checklist requirement on 6 different templates, not just a cosmetic gap. **Still open.**
- `solution.related` (4 cross-refs per solution in `solutions.json`, meant for the sidebar's "related solutions/products" per design §5.6) is completely unused by both `pages.js` and `solution-detail.html` — dead data field. **Still open.**
- ~~`category.selectionGuideDownloadHref`/`category.selectionGuideHref` exist in `products.json` for all 4 categories but `product-category.html` never renders the "Download Category Selection Guide" button or "Selection Guide →" link the design calls for.~~ — **`selectionGuideHref` fixed** during T8.2's products.json orphan-field batch (e4a3afb): now renders as a "Selection Guide →" link. `selectionGuideDownloadHref` remains deliberately unwired — the PDFs it points to don't exist on disk (`src/assets/docs/` is empty), tracked below in E.
- `support.json` articles' banner/cover image fields are never rendered inside `tech-detail.html`'s article body — the only image on a tech-detail page is the author avatar. **Still open.**

**C. Structured-data (JSON-LD) field gaps — all fixed as side effects of T8.2 batches, re-verified just now via grep, not just recalled:**
- ~~home.html's `Organization` JSON-LD is missing `logo` and `contactPoint`~~ — fixed (ea42b34, T8.2 §6.A batch): both now present, plus `foundingDate`/`areaServed` added too.
- ~~product-detail.html's `Product` JSON-LD is missing the `sku` field~~ — fixed (e4a3afb, T8.2 products.json orphan-field batch).
- ~~product-category.html's per-model `Product` JSON-LD blocks also lack `sku`~~ — fixed (e4a3afb, same batch) — plus both templates also gained `image`/`category`, and their `url`/`offers.url` were later made absolute (590dcf0, 8c89186).

**D. Real dead links found (beyond the already-tracked ~17):**
- ~~product-detail's alternate/companion-parts carousel references at least 3 more non-existent product slugs: `IKW50N65EL5`, `1ED020I12-F2`, `BSC0901NS`~~ — fixed (e95bc7f, T8.2's 3-severe-fixes batch, the "13 dangling PartRefs" fix): re-verified just now — `1ED020I12-F2` has exactly 1 remaining match in `products.json`, but it's plain prose text in an FAQ answer ("...devices such as the Infineon EiceDRIVER 1ED020I12-F2 are fully compatible"), not an `href`-bearing `PartRef` link, so it's not a dead link. All 3 confirmed resolved.
- `src/assets/docs/` **does not exist at all** — tech-detail's "Related PDF Download" sidebar links (4 articles × 1 PDF each) point to files that were never created anywhere in the repo. This is a new, previously-unrecorded instance of the same "missing asset" pattern as the ~13 missing SVGs below, not a link-checker-catchable dead link (it's a `/assets/...` path, which `links.js` explicitly skips per its own design). **Still open** — same root issue as `selectionGuideDownloadHref`'s missing PDFs above.

**E. Missing SVG asset files — same pattern as the already-tracked ~13, found via this audit's own file-existence checks (may overlap with the existing tracked count, needs reconciliation, not simple addition):** home.json's Why-Choose-Us icons (`inventory.svg`, `fae.svg`, `genuine.svg`, `logistics.svg`) and Hero trust badges (`badges/parts.svg`, `badges/quote.svg`, `badges/distributor.svg`) don't exist on disk — same root defect as the already-tracked about.json SVG gap, just a different template surfacing it (home.html renders these `<img>` tags too). Also: both FAE author-page avatars (Li Wei, Chen Jing) currently point to the same generic `about-hero.svg` rather than distinct named avatars — not a missing file, but a content/asset-authenticity gap (E-E-A-T requirement per PRD §3.9).

**F. Smaller content/wording gaps (Low, batch-fixable when picked up):**
- ~~4 product-category page titles + support overview page title omit "BeiLuo" or "Infineon" respectively~~ — fixed (Tier 1 batch, c6cbcae): category titles gained `| BeiLuo`, support overview gained an "Infineon" prefix.
- ~~author-page titles ("Li Wei — BeiLuo FAE Profile | BeiLuo") omit "Infineon"~~ — fixed (Tier 1 batch, c6cbcae): the redundant second "BeiLuo" mention replaced with "Infineon".
- ~~CTA button text/component mismatches vs. design wireframe examples: product-detail's primary CTA says "Request a Quote" (not "Get a Quote"); products-list/solutions-list "View Models"/"Read" links render as solid CTA buttons instead of the ghost/text links the design calls for.~~ — fixed (Tier 1 batch, c6cbcae): product-detail's hero CTA now says "Get a Quote"; `products-list.html`'s category-card link now uses `.btn--secondary` (ghost/outline), matching `solutions-list.html`'s already-correct equivalent.
- ~~product-detail's bottom multi-CTA section has only 2 actions... vs. the ≥4 the design calls for~~ — fixed (Tier 1 batch, c6cbcae): now renders all 4 (Download Datasheet / Apply for Sample / Get a Quote / Ask FAE), matching design.md §5.4 exactly; "Back to Category" dropped (not one of the 4 specified actions, and the page's own breadcrumb already covers that navigation).
- ~~1 tech-detail article's meta description is 172 chars (over the 160 soft cap)~~ — **re-checked, already resolved**: all 4 articles in `support.json` are currently 141–155 chars, within the cap. Likely fixed incidentally by an earlier content batch; no action taken this pass.
- ~~`about.schema.md` says `advantages` should be 3–5 items; `about.json` has 6~~ — fixed (9162d67, T8.2 §6.B/C batch): merged to 5, no detail lost.

**Not a defect (confirmed during audit, no action needed):** category/product-teaser icon `alt=""` + `aria-hidden="true"` pairing is intentional/correct per WCAG (decorative icon beside visible text label) — T7.5 already audited and confirmed this site-wide; the check_list1.md wording asking for "alt contains category keyword" on these specific icons is the one place check_list1.md's own aspirational wording conflicts with the (correct) accessibility-driven implementation choice — worth a doc note in check_list1.md itself if this is revisited, not a code fix.

### T8.2 Audit Findings (2026-07-05) — NEW TODO CANDIDATES, not yet implemented

4 parallel agents independently checked every `check_list2.md` item (7 JSON files × D1–D7) against actual field values and template consumption, not just `validate-data.js`/`npm test` passing. The 3 most severe items (support.json category distribution, 13 dangling PartRefs, metaDescription overlength) are fixed — see §3. Everything below is **recorded as a candidate, not yet implemented.**

**A. Orphan data fields — largest category, mostly in site.json/home.json.** These fields have real, correct values but are never actually read by any template (confirmed via grep across `src/templates/`):
- ~~`site.json`: `nav.items` never looped; `navCategories` never assigned (Mega Menu dead); `footer.columns`/`copyright`/`sitemapHref`/`robotsHref` orphaned; `contact.whatsappHref` unused; `seo.siteName` never generated an `og:site_name` tag; `Organization` JSON-LD missing `logo`; `description` using `oneLiner` instead of `slogan`~~ — **fixed** (pending commit): see §3's "second batch" for the full writeup, including a bonus-discovered home/about `seo`-merge bug fixed along the way.
- **Still open, deliberately deferred (bigger than data-wiring):** `contact.wechatQrSrc` is still unused — discovered while fixing the above that this isn't just an unread field, `contact-float.html`'s WeChat button (`data-wechat-toggle`) has **zero corresponding JS handler anywhere in `src/assets/js/`**, i.e. clicking it does nothing at all. Wiring the data field is the easy part; this needs new toggle JS, a QR-popup markup/CSS treatment not specified anywhere in design.md that's been read this session, and a design decision on popup layout — recommend its own small task rather than folding into more orphan-field cleanup.
- ~~`home.json`: `hero.bgSvgAlt` is hardcoded to `alt=""` in the template regardless of the field's actual value... `solutionsTeaser[].industry` (badge) never rendered; `supportTeaser[].category` (badge) and `.date` never rendered~~ — fixed (Tier 1 batch, c6cbcae): template now reads `{{hero.bgSvgAlt}}` (zero visual change since the field is currently empty, but correctly responds to the data now); solutions-teaser cards show the industry badge (reusing `.solution-card__industry`); support-teaser cards show a category badge (a new `categoryLabel` field was added to each `home.json` entry, matching `support.json`'s human-readable category names, since the raw `category` slug isn't fit for display) and the date. `newsTeaser[].type` re-confirmed **not a defect** — it's a redundant machine-readable duplicate of the already-rendered `categoryTag` display string, not meant to be shown separately.
- ~~`products.json` (already noted in T8.1's own findings, re-confirmed independently here): `category.selectionGuideHref`/`selectionGuideDownloadHref` never rendered; `model.shortDescription` never shown in the visible detail-page UI (only leaks into JSON-LD `description`); `category.title` not used as the detail-page H1 (template hardcodes a different string); `Product` JSON-LD missing `sku`/`image`/`category` fields (schema.md's own JSON-LD Mapping table documents all three).~~ — **mostly fixed** (e4a3afb): `selectionGuideHref` now rendered as a "Selection Guide →" link (`selectionGuideDownloadHref` deliberately left unwired — the PDFs it points to don't exist on disk, see §3's "fourth batch"); `shortDescription` now visible in the detail-page info bar; `Product` JSON-LD gained `sku`/`image`/`category`. `category.title` turned out to already be correctly wired as the `<title>` tag (not a true orphan) — using it for the visible H1 too isn't required, that's normal title-vs-H1 differentiation, not a defect.
- ~~`Product` JSON-LD's `url`/`offers.url` fields were relative paths, should be absolute per `products.schema.md`~~ — fixed (590dcf0): see §3's "fifth batch" — solved the `{{#each}}` parent-scope blocker by computing `absoluteHref` per-model in `pages.js` (same pattern as the `navCategories` fix).
- ~~No automated regression test covered the `Product` JSON-LD absolute-URL invariant~~ — fixed (732165d): see §3's "sixth batch" — added a `tests/pages.test.js` assertion, verified with a real red-green cycle (twice — once by the controller, once independently by the reviewer).
- ~~`ItemList` JSON-LD's `itemListElement[].url` (`pages.js` line ~155, a separate JSON-LD block from `Product`) was also relative~~ — fixed (pending commit): see §3's "seventh batch" — pure-JS one-line fix (no template `{{#each}}` involved, so no parent-scope workaround needed this time), plus its own regression test, both red-green verified twice.
- ~~`solutions.json`: `solution.blockDiagram.src`/`.alt` is dead data — `solution-detail.html` actually renders a *different*, undocumented field (`solution.diagramSrc`/`.diagramAlt`) with identical current values, masking the mismatch~~ — fixed (Tier 1 batch, c6cbcae): removed the redundant `blockDiagram` object from all 5 solutions; `solutions.schema.md` updated to document `diagramSrc`/`diagramAlt` (the fields actually rendered) instead. `solution.related` (already tracked from T8.1) confirmed still unused — that's Tier 4, not part of this batch.

**B. Content-quality gaps (D3/D4) — genuine, not checklist-wording nitpicks:**
- ~~`home.json`: `hero.headline` = "Your Trusted Infineon Distributor" is the exact generic template phrase the checklist explicitly warns against, with no "BeiLuo" mention at all; `productsTeaser[sensors].summary` is the only one of 4 category teasers missing the literal "Infineon Sensors" keyword pairing the other 3 use; `solutionsTeaser[industrial-automation].summary` is the only one of 3 solution teasers with no BeiLuo/distributor-context sentence.~~ — **fixed** (pending commit): `hero.headline` is now "BeiLuo — Your Trusted Infineon Authorized Distributor" (turns out this exact generic phrase was literally the example text in `home.schema.md`'s own field table — whoever authored the content apparently copied the placeholder verbatim); the other two teaser summaries reworded per finding.
- ~~`news.json`: both "company"-type articles are written in third-person journalistic style (zero "we"/"our" usage)... both "industry"-type articles cite only vague, unattributed claims...~~ — **fixed** (pending commit), with one correction from re-verification: both company articles (`beiluo-expands-infineon-igbt-stock-2024`, `beiluo-fae-support-program-launch`) were fully rewritten to first-person voice (996/1007 words, 36/45 "we"/"our"/"us" uses, all H2 ids/internal links/word counts preserved — verified by an independent reviewer's own script, not just re-reading). On the "both industry articles" claim: **only one actually had the problem** — `wide-bandgap-semiconductors-market-outlook`'s "Market analysts estimate..." sentence was unattributed and is now cited to Yole Group (a real compound-semiconductor market research firm, not fabricated); `infineon-aurix-mcu-automotive-applications` was independently re-read twice (once by me, once by the reviewer) and has no comparable unattributed-claim issue — the original finding overstated this to "both."

**C. Count-quota violations (D5), beyond the 3 already fixed:**
- ~~`solutions.json`: all 5 solutions' `bomList` has only 2 entries; schema quota is ≥3.~~ — **fixed** (pending commit): each of the 5 solutions got 1 more real (not fabricated) product from `products.json`'s existing 8 models, with a fresh, logically-sound pairing rationale (e.g. `TLI4966G` as the rotor-feedback sensor for `bldc-motor-drive`'s FOC loop). Side note surfaced by review: `validate-data.js` currently only enforces `bomList.length >= 2`, not `>= 3` as `solutions.schema.md` documents — a pre-existing code/doc mismatch, not introduced by this fix (this fix moves the data toward the doc's stricter standard regardless); worth tightening `validate-data.js` in a future pass.
- ~~Also, the 5 solutions' industry coverage is `Motor Drive, EV Charging, Industrial Automation, Industrial Automation, Solar PV` — "Industrial Automation" duplicated... while "Home Appliance"... is entirely missing.~~ — **partially fixed, by explicit user decision** (pending commit): asked the user how to handle this via `AskUserQuestion` since neither duplicated solution's actual content (`industrial-iot-sensor`: an IoT sensor node; `mcu-embedded-control`: MCU control + automotive safety) honestly reads as "Home Appliance," and forcing that label on either would create a label/content mismatch. User chose "fix the duplication only, don't force-fill Home Appliance." `mcu-embedded-control`'s industry changed to "Embedded Control" (matches its own title, no fabrication). The "Home Appliance" gap remains open — writing a real 6th (or replacement) solution for it is a distinctly bigger task (~T4.3 scale: full ≥800-word body, BOM, block diagram, etc.), tracked here as its own candidate, not attempted in this pass.
- ~~`solutions.json`: 2 of 5 solutions' `blockDiagram.alt` is under the 40-word minimum (35 and 36 words).~~ — **fixed** (pending commit): both expanded to reference the newly-added BOM entries, keeping `diagramAlt` (the field actually rendered) and `blockDiagram.alt` (the schema-documented but currently-dead duplicate field, see §6.A above) in sync — both now identical, ≥40 words (41-52 across all 5 after the expansions).
- ~~`about.json`: `advantages` array has 6 items, exceeding the 3–5 quota...~~ — **fixed** (pending commit): merged the two most thematically-overlapping items ("Fast Global Delivery to 40+ Countries" and "24-Hour Competitive Quotation," both about response/delivery speed) into one "24-Hour Quotation and Fast Global Delivery" entry that preserves every concrete detail from both (carriers, country count, response/delivery times, blanket-order terms) — no information lost, array is now 5 items.
- `support.json`: all 4 `SupportCategory` (category-index-page) `metaDescription` values checked this session — all 124-138 chars, well within the 155-char limit. **No action needed, confirmed clean.**

**D. Smaller placeholder/content issues:**
- `solutions.json`: `blockDiagram.src` is the identical literal path for all 5 solutions (`/assets/svg/illustrations/solution-diagram.svg`) rather than the schema-required per-slug `bd-<slug>.svg` pattern — same root cause as the already-tracked "shared generic illustration" asset gap (§E below), just documented from the JSON-content side this time.
- `news.json`/`support.json`: all articles across both files reuse the identical `news-hero.svg` illustration (only 5 distinct illustration SVGs exist in the whole repo: `about-hero`, `contact-hero`, `news-hero`, `solution-diagram`, `trust-badge`) — acceptable per the checklist's literal "real path, not placeholder" wording, but a real visual-differentiation gap worth flagging alongside the missing-SVG-asset items already tracked.

**Not defects (confirmed during audit, recorded for traceability only):** site.json's `brand.oneLiner` reads slightly generic but does mention Infineon (soft/non-blocking); `home.json` has an undocumented-but-correctly-wired `trustBar` field not mentioned in `home.schema.md`'s field table (schema doc gap, not a code defect); `productsTeaser` "View Models →" link text renders as a solid CTA button rather than a ghost link (a design-implementation choice, not an orphan-field issue, already noted in T8.1's findings).

### High severity — should be triaged before T9/launch

| Source | Issue |
|--------|-------|
| T4.6/T3.x | `about.json`'s `advantages[]` (6 icons), `cases[]` (4 logos), `customsDeclarations[]` (3 images) reference ~13 SVG files under `src/assets/svg/icons/` and `src/assets/svg/illustrations/` that **do not exist on disk** (confirmed via `fs.existsSync`; only `about-hero.svg` exists). `about.html` is the first template to render these as visible `<img>` tags, so the About page's advantage icons, client logos, and — critically — the **PRD §3.7-mandated customs-declaration trust section** currently show broken images. Needs a dedicated SVG-asset-generation task (extends T3.x scope). |
| T4.2/T4.6, surfaced when Phase 5 completed | The first successful end-to-end `node src/build.js` run surfaced **~17 distinct real dead links** (~25 occurrences), separate from the known sitemap.xml/robots.txt false positive below: (1) `home.html`'s Solutions/Support teasers link to `/solutions/motor-drive/`, `/solutions/ev-charging/`, `/solutions/industrial-automation/`, `/support/application-notes/infineon-coolmos-pfc-application/` — none of which exist in `solutions.json`/`support.json`; (2) `product-detail.html`'s `alternativeParts`/`companionParts` in `products.json` cross-reference part-number slugs with no matching real model (e.g. `tc264da`→`tc265da`, `xmc4700-...`→`xmc4800-...`, `ikw40n120h3`→`ikw50n65el5`, several MOSFET/sensor cross-refs). Predates Phase 5 entirely — masked until now because `build.js` always failed early at a missing template before reaching link validation. This is squarely **T9.1**'s job ("全量构建+死链/空链零容忍校验"); flagged here so it doesn't surprise that task. Fix requires either adding the missing product/solution entries or correcting the dangling slug references. |
| T2.4, unblocked now | `navCategories` was never wired into `pages.js`'s page context (derived from `products.categories`, `featuredModels` = first 2 models per category) — nav mega-menu currently renders 0 categories site-wide, gracefully but incompletely. Was deliberately deferred to "after Phase 5 template work" — **Phase 5 is now done**, so this is unblocked and should be picked up soon (candidate for T7.x or an earlier ad-hoc fix, since it affects every page's nav on every already-shipped template). |

### Medium/Low severity — triage at T8.1/T8.2 final sweep

| Severity | Source | Issue |
|----------|--------|-------|
| Low | T0.1 | `README.md` directory tree lists `src/build.js` before it existed — self-resolved now. |
| Low | T4.7 | `milestones-alternative` test couples to real `about.json` layout (`history` key). |
| Low | T4.7 | `logo.src/alt` allow whitespace-only strings (uses `=== ''` not `nonEmptyString`). |
| Low | T4.7 | No edge-case tests for `validateData(null/undefined/{})`. |
| Low | T4.7 | Missing failure-mode tests for `contact.wechat`, `seo.siteName`, `logo.alt/width/height`, `jsonLd.organizationType`. |
| Low | T5.2 | Triple-brace `{{{seo.title/description}}}` in JSON-LD — safe (pages.js builds from validate-data-checked fields only); revisit if a CMS ever feeds these fields. |
| Low | T5.6 | Category-index/tag-page breadcrumb *display* (nav breadcrumb via `pages.js`'s `markCurrentLast`) is shallower than the BreadcrumbList JSON-LD authored in `support-list.html` — same pattern already present for `product-category` pages, likely intentional but worth confirming at T8.1. |
| Low | T4.4 | `support.json` data defect: article `infineon-optimos-mosfet-overview`'s `internalLinks` declares a model link to `/products/mosfet/irfs4321pbf/` that never actually appears in the article body text — `validate-data.js` doesn't cross-check `internalLinks[].href` against body content, so nothing catches it. |
| Low | T5.6/T5.7 | Tag badges (`support-card.html`, `tech-detail.html`) render the raw tag slug (`igbt`) instead of the human-readable `tag.name` (`IGBT`) — the per-article contexts only carry tag slugs, not resolved `Tag` objects. |
| Low | all list pages | `{{> sidebar}}` renders empty on every page that includes it (products-list/solutions-list/support-list/news-list/product-category/solution-detail) because `pages.js` never injects `sidebarSections` anywhere site-wide. |
| Low | T5.6/T5.7/T5.8/T6.1 | New `pages.js` context-enrichment fields added across T5.6–T6.1 (`guidesArticles`.../`authorInfo`/`relatedArticlesResolved`/`companyArticles`/`industryArticles`/`latestNews`/`authorType`/`overlayClass`/`shareTitleEncoded`/`shareUrlEncoded`/`rowCells`) have no committed unit test coverage — `tests/pages.test.js` uses a minimal stub fixture that doesn't exercise these; correctness was verified via disposable scripts during each review only. Candidate for T9.1/T9.4 coverage pass. |
| Low | T6.1 | `table-filter.js`'s range filter treats non-numeric/blank cell values as always-matching — an edge case, not currently reachable (no shipped model has a missing numeric param), worth tightening if empty numeric cells become possible later. |
| Low | T6.2 | `product-detail.html`'s `.tab-container` retains a leftover `data-tabs="product-detail"` attribute and the tablist div carries an extra `class="tab-list"` not mentioned in `markup-contract.md` §2 — both inert (unused by `tabs.js`, which selects by ARIA role), purely cosmetic. |
| Low | T4.6 | `about.schema.md` specifies `advantages` should be 3–5 items; `about.json` has 6 — non-blocking. |
| Low | T2.2/T6.4 | `markup-contract.md` §4 附录速查表 assigns `.form-group` layout to "CSS 负责", but `style.css`'s Form Base section (T2.2) never added a `.form-group` rule — the class is present in `contact.html` markup but unstyled (relies on default block spacing). Purely cosmetic, not fixed in T6.4 scope (out of that task's boundary). |

**Fixed this session** (kept here for traceability, not action items):
- ~~`product-detail.html` Tab markup deviated from `markup-contract.md` §2~~ — fixed T6.2 (e551dfc).
- ~~`product-category.html` tbody was hardcoded, silently dropping dynamic columns~~ — fixed T6.1 (eafdec4).
- ~~`news.json` article bodies were plain markdown text, not HTML~~ — fixed T5.8 (ea5b726).
- ~~`about.json` `team[]` referenced a fabricated author not in `support.json`~~ — fixed T5.9 (ecefabf).
- ~~`/about/authors/` breadcrumb crumb was a dead link~~ — fixed T5.9 (ecefabf).
- ~~`contact.html` success message had a fabricated `mailto:` address~~ — fixed T5.10 (4b5f1e0).
- ~~`toc.js` scroll-highlight only flashed briefly instead of staying active while reading a section~~ — fixed T6.3 (53c6c8f).
- ~~`style.css` styled `.is-error` but `markup-contract.md` §4 mandates JS write `.is-invalid` (unused selector, never referenced anywhere)~~ — fixed T6.4 (5b66e86).
- ~~`product-detail.html`/`solution-detail.html` used double-brace escaped interpolation inside JSON-LD `<script>` blocks~~ — fixed T7.2 (f32001c), plus 2 more templates (`product-category.html`/`solutions-list.html`) with the same defect discovered during the same audit.
- ~~`breadcrumb.html`'s `{{breadcrumbJsonLd}}` hook was never populated by `pages.js` — dead code~~ — fixed T7.2 (f32001c): now populated for the 4 templates that previously had zero BreadcrumbList JSON-LD.
- ~~`links.js`/`build.js` sitewide false positive: `footer.html`'s links to `/sitemap.xml`/`/robots.txt` flagged dead on every page~~ — fixed T7.3 (c373abd).
- ~~`contact.html`'s business-hours/response-time text was a specific, unbacked claim with no corresponding `site.json` field~~ — fixed T7.4 (f21a8be): added `contact.businessHours`/`contact.responseTime` fields.
- ~~`build.js` never copied `src/assets/**` to `dist/assets/` — deployed site would have zero CSS/JS/images~~ — fixed T8.1 (4e10068).
- ~~`solution-detail.html` rendered `solution.body` as raw unconverted Markdown (`##` headers visible as literal text)~~ — fixed T8.1 (4e10068): converted all 5 solutions to HTML, added `.longform` class.
- ~~`pages.js`: 10 page types built a `breadcrumb` array missing that page's own crumb level, so visible nav breadcrumb had fewer levels than the same page's JSON-LD~~ — fixed T8.1 (4e10068).
- ~~`support.json`: 4 articles distributed `guides×2, application-notes×2, troubleshooting×0, reviews×0` — 2 category pages rendered empty~~ — fixed T8.2 (e95bc7f): honestly recategorized 2 articles (not fabricated), all cross-references to their old URLs updated.
- ~~`products.json`: 13 dangling `PartRef` cross-references across all 8 models (extends the 3 known from T8.1 to 13 total)~~ — fixed T8.2 (e95bc7f): removed or replaced with real models, 0 dangling/self-references remain.
- ~~`support.json`/`news.json`: several `metaDescription` values over the 155-char schema limit~~ — fixed T8.2 (e95bc7f): all 8 articles across both files now ≤155 chars.
- ~~`nav.items`/`navCategories`/`footer.columns`/`contact.whatsappHref`/`seo.siteName` all orphaned (never rendered); `Organization` JSON-LD missing `logo`/`contactPoint`, wrong `description` source field~~ — fixed T8.2 (ea42b34): nav/footer/contact-float/head-meta/home.html all made data-driven, see §3/§6.A.
- ~~`pages.js`: home/about pages' `seo` context object silently lost `siteName`/`defaultTitle`/`defaultDescription`/`baseUrl` due to a shallow-spread key collision with their own `home.json`/`about.json` flat `seo` field~~ — fixed T8.2 (ea42b34): discovered as a side effect of the `og:site_name` fix above, not a pre-planned finding.
- ~~Site copy's "Top 8" ranking claim worded/scoped inconsistently across `about.json`/`home.json`/`products-list.html` ("Infineon distributor" vs. generic "Electronic Component Distributor"; "China" vs. "Asia-Pacific")~~ — fixed (79092e3): unified to `prd.md`/`design.md`'s documented canonical phrase, user-flagged (not one of T8.2's official 35 findings).
- ~~`home.json`'s `hero.headline` was the literal example text from `home.schema.md`'s field table; `productsTeaser[sensors]`/`solutionsTeaser[industrial-automation]` summaries missing keyword/BeiLuo-context; `news.json`'s 2 company articles were third-person; 1 industry article had an unattributed market-size claim; `solutions.json`'s `bomList` under-quota + `industry` duplicated; `about.json`'s `advantages` over-quota~~ — fixed T8.2 (9162d67), see §3's "third batch".
- ~~`products.json`: `category.selectionGuideHref` never rendered; `model.shortDescription` never shown in visible UI; `Product` JSON-LD missing `sku`/`image`/`category`~~ — mostly fixed T8.2 (e4a3afb), see §3's "fourth batch" (download-guide button deliberately deferred, reason documented there).
- ~~`Product` JSON-LD's `url`/`offers.url` were relative, should be absolute per `products.schema.md`~~ — fixed T8.2 (pending commit), see §3's "fifth batch".

---

## 7. Codex Re-check Conclusions

Codex re-check is **MANDATORY** after every task (user rule, established 2026-06-30). Summary of outcomes, most recent first:

| Task | Codex Result |
|------|-------------|
| T8.1/T8.2 Tier 1 (data wiring + title/CTA fixes) | **Approved.** Same EPERM-on-write sandbox limitation, confirmed via pre-built `dist/`. Static audit script confirmed: zero remaining `blockDiagram` references anywhere in `src/`/`tests/`, `diagramSrc`/`diagramAlt` present on all 5 solutions, `supportTeaser`'s `categoryLabel` values match `support.json` exactly, home/product-detail/products-list rendered HTML contain the expected badges/CTA text/button classes, and all 4 touched `<title>` tags contain the expected keywords. Read the `pages.js` and `product-detail.html` diffs directly, confirming only the described title-string and CTA changes, nothing else touched. |
| T9.2 extension (10 more contrast instances + cta-block__text) | **Approved.** Same EPERM-on-write sandbox limitation, confirmed via pre-built `dist/`. Static audit script confirmed all 11 named selectors now use `--c-primary-dark`/`--c-footer-heading` and a regex sweep confirmed zero remaining `color: var(--c-primary);` + `background: var(--c-primary-tint)` pairs in the same rule anywhere in the file. Read the full diff directly — confirmed all 11 changes are exactly the described token swaps, nothing else touched. |
| T9.2 (real-browser overflow/AA/touch-target fixes) | **Approved.** Confirmed via a pre-built `dist/` (same EPERM-on-write sandbox limitation as prior rechecks) plus a static audit script checking CSS brace balance and presence of every named fix (nav `min-width:0`, tablet media query, `.related-articles` grid-column, layout-with-sidebar/tech-detail-layout min-width fixes, `.btn--sm` 44px, both new `-dark` tokens and their exact usage counts) — all passed. Additionally read the full diff directly and confirmed every changed line traces to one of the 9 described browser-found issues, with no unrelated speculative styling. |
| T9.1 (home.json dead-link fix) | **Approved.** First attempt hit the 3-minute tool timeout mid-verification (same class of issue as the "Top 8" recheck) after already confirming `git status` scope (only `home.json` + task-ledger docs) and that `dist/`'s 4 target files existed with real content; follow-up citing that partial evidence and asking only for the 2 remaining empty-link scans + final verdict got a clean APPROVE. Confirmed via an equivalent read-only scan (PowerShell's `Select-String` here doesn't support `-Recurse` the way the prompt's example assumed, so Codex substituted `Get-ChildItem -Recurse \| Select-String`, same effect): `href=""` count 0, `href="#"` count 0. |
| T8.1 CSS batch (page-specific CSS gap) | **Approved.** Same sandbox EPERM limitation; ran the audit script against the controller's already-built `dist/`. Confirmed CSS brace balance, all 4 class-name-mismatch fixes (`btn--outline`/`specs-table`/`btn--primary` remaining counts all 0), `stock-badge--inStock`/`--rfq` casing exactly matches `products.json`'s real values, and both reviewer-caught fixes (`card__tags`, `solution-diagram__img`) present. Also spot-read the new CSS directly, confirming heavy `var(--...)` token usage (359 hits in the new sections) with no hardcoded hex colors introduced. Internal reviewer independently rewrote (not reused) the full 52-page HTML/CSS class cross-reference script, individually judged all 55 candidate gaps, and caught 1 Major (`.card__tags`) + 1 Low (`.solution-diagram__img` not centered) that were fixed before Codex sign-off. **Both this recheck and the reviewer explicitly did not attempt browser/visual verification — no browser tool exists in this session — and said so up front rather than silently skipping it.** |
| `ItemList` absolute URL + its test | **Round 1: transient network outage** — `codex doctor` confirmed both `websocket` and `reachability` failing (same signature as T7.5), resolved by waiting ~4 min and retrying. **Round 2: spurious REJECT** — flagged `CLAUDE.md` appearing in `git diff --name-only` as scope creep, an almost-exact repeat of the T7.4 false objection over the same file; that single-line comment change has been sitting uncommitted since before this session started. **Round 3: Approved** after citing the T7.4 precedent and re-cleaning stray `dist/` build artifacts that were also inflating `git status`; Codex additionally ran `node tests/pages.test.js` directly (50/50 pass — this single-process invocation doesn't hit the `node --test` per-file-spawn EPERM) and its own independent script confirming 0 relative urls across the real category pages. |
| Absolute-URL regression test | **Round 1: spurious REJECT** — Codex's sandbox can't spawn child processes for `node --test` (same `spawn EPERM` limitation as T7.4), so `npm test` failed 9/9 files immediately, before any real test ran, and Codex initially treated this as a genuine failure. **Round 2: Approved** after a same-task clarification citing 2 independent real-environment 364/364 results (controller + reviewer, the latter having independently red-green-verified the new test) and an explicit instruction not to re-run `npm test`; Codex then read the diff/implementation directly and reversed its verdict. Exact recurrence of the T7.4 pattern — see the Fourth tooling note. |
| `Product` JSON-LD absolute URLs | **Approved.** Same sandbox EPERM limitation; ran the audit script against the controller's already-built `dist/`. Confirmed all 16 `Product` blocks' `url`/`offers.url` start with `https://www.beiluo.com` (0 relative). Internal reviewer wrote its own script rather than trusting mine, spot-checked path correctness on multiple products, and confirmed `jsonLd.organizationUrl`'s value is genuinely referenced (not a hardcoded string). Reviewer's 3 Low-priority notes (a pre-existing schema.md wording inconsistency, the separate `ItemList.url` still being relative, no dedicated regression test) recorded as new candidates, none blocking. |
| `products.json` orphan fields | **Approved.** Same sandbox EPERM limitation; ran the audit script against the controller's already-built `dist/`. Confirmed all 4 category pages' Selection Guide links resolve to real support articles with no download-button/PDF reference rendered, and all 8 detail pages have both a visible `shortDescription` and a valid `Product` JSON-LD with `sku`/`image`/`category` populated. Internal reviewer independently confirmed `src/assets/docs/` doesn't exist and read `links.js`'s actual implementation to verify it skips `/assets/` paths (rather than trusting the claim), and independently tested the `{{applications.0}}` array-index template syntax against `render.js` directly. Reviewer's 1 Minor finding (new `url` field should be absolute per schema) was investigated but reverted rather than partially fixed — see §3/§6.A for why. |
| T8.2 (§6.B/C content-quality + count-quota) | **Approved.** Same sandbox EPERM limitation as prior T8.2 rechecks; ran the audit script against the controller's already-built `dist/`. Script confirmed both rewritten news articles ≥800 words with 36/45 first-person pronoun uses, all 5 solutions' industries unique, all `bomList`s length-3 with real `partNo`s, `diagramAlt`/`blockDiagram.alt` in sync and ≥40 words, `about.json` advantages at 5. Internal reviewer went further: independently re-read the second industry article to verify it genuinely has no sourcing problem (rather than trusting the "corrected from the original finding" claim), scripted H2-id/toc/internal-link preservation checks on both rewritten news bodies, and a git-stash dead-link before/after comparison (unchanged). |
| "Top 8" wording consistency | **Approved.** Same sandbox EPERM limitation as before, but this time also hit a fresh issue: the first attempt ran every verification command successfully (confirmed correct output for all 4 assertions) but got cut off by the tool's 3-minute timeout before Codex could type its final APPROVE/REJECT sentence. A second, leaner request that cited the exact prior command output and asked only for the verdict got a clean APPROVE without needing to re-run anything — see the 7th tooling note below. |
| T8.2 (§6.A orphan-field wiring) | **Approved.** Same sandbox-write limitation as the previous T8.2 recheck (`rmSync` OK, `node src/build.js` hit `EPERM`), so it ran the audit script against the controller's already-built `dist/`. Script confirmed across all 52 pages: exactly 1 `data-megamenu-trigger` and 7 nav links per page, 4 footer columns per page, `og:site_name` present on all 52, Organization JSON-LD's `logo`/`contactPoint`/`description`-from-slogan all correct. Internal reviewer went further with a real from-scratch build (not relying on the controller's), a `git stash` before/after dead-link comparison (identical 4-item list both times, confirming no regression), byte-level comparison of Mega Menu output against `products.json`'s real model data, and manually re-read all ~13 page-construction blocks in `pages.js` to independently confirm the home/about `seo`-merge bug's scope (only those 2 pages were affected) rather than trusting the claim — found 1 additional Minor/Low latent-risk instance (`support-list`'s `...support` spread ordering) which was hardened before Codex sign-off. |
| T8.2 (3 data fixes) | **Approved.** Its own sandbox couldn't rebuild `dist/` fresh (`rmSync` succeeded but the subsequent `node src/build.js` hit `EPERM` on write — a read-only-sandbox limitation, not a data problem), so it ran the audit script against the already-built `dist/` plus direct `src/data/*.json` reads; the core assertions (0 dangling PartRefs, 0 self-references, correct 1-per-category distribution, 0 over-length metaDescriptions, 0 residual old URLs) all come from source-data reads, unaffected by the stale-dist caveat. Controller independently did a truly clean `git clean -fdx dist/` rebuild beforehand confirming the same category-directory counts. Internal reviewer went further: `git stash`-based before/after dead-link comparison (24→4, confirming the net improvement), tag-consistency check (no orphaned "selection-guide"/"application-note" tags left on the recategorized articles), and a self-reference check (no model listing itself as its own alternative/companion) — caught 1 Minor (a leftover "this application note" phrase in the recategorized troubleshooting article's closing paragraph) which was fixed before Codex sign-off. |
| T8.1 (3 systemic fixes) | **Approved**, first attempt with the file-based script (no tooling issues). Verified: `dist/assets/` has 32 files including `style.css`/`tokens.css`/`form.js`; 0 raw Markdown remaining in any of the 5 solution-detail pages, 0 missing `.longform` class; all 52 pages checked, 0 breadcrumb-depth-vs-JSON-LD mismatches. Internal reviewer independently ran an even more rigorous pass first: SHA-256 hash comparison of every copied asset file (byte-identical), character-level diff of Markdown-stripped-vs-HTML-stripped body text (0 content loss from the conversion), and crumb-name cross-check against every template's pre-existing hand-authored JSON-LD (confirmed "About Us" and other exact wordings match). Both independently reached APPROVE with zero Critical/Major findings. |
| T7.5 | **Approved**, after 3 attempts failed on Codex-side websocket connectivity errors unrelated to the task (`codex doctor` confirmed `websocket`/`reachability` failures at the time — a transient network condition, not a quoting or sandbox issue). 4th attempt succeeded cleanly with the file-based script: 172 imgs, 0 missing alt, 0 empty-alt-without-aria-hidden (the key pass/fail signal), 26 correctly-decorative empty alts, 146 descriptive alts, 0 generic placeholders, 0 inline SVGs lacking accessibility treatment. Internal reviewer independently re-ran the entire audit from scratch (not trusting the numbers) and got identical results. |
| T7.4 | Round 1: **REJECT**, but on false grounds — Codex's own `-s read-only` sandbox couldn't spawn Node's per-test-file child processes (`spawn EPERM`), reported as "npm test unconfirmed" and also flagged `CLAUDE.md`/`todo_write.md` appearing in `git diff --name-only` as scope creep. Both objections were addressed with evidence in a same-task clarification follow-up: 3 independent real-environment `npm test` runs (2 by the controller, 1 by the internal reviewer) all showed 363/363 pass; `CLAUDE.md`'s diff was a pre-existing unrelated change and `todo_write.md`'s diff was the single expected `pending`→`in_progress` status line. Round 2: **Approved** after reviewing this evidence — the core audit (FAQ structure, contact-text byte-for-byte integrity via `git stash` diff, no `Missing field` errors, diff scoped to 3 files) had already passed in round 1 and remained the basis for the reversed verdict. |
| T7.3 | **Approved**, first attempt with the file-based verification script (no findings from the T7.2 tooling issue this time). Confirmed: `dist/sitemap.xml` well-formed with 52 absolute-URL `<loc>` entries, `dist/robots.txt` has all 3 required lines, `[dead] /sitemap.xml`/`/robots.txt` false positives dropped from 104 to 0, remaining 25 dead-link lines unchanged (pre-existing, T9.1-scope). Internal reviewer separately verified the same numbers plus reproduced the before/after delta via `git stash` (129 total dead lines before the fix, 25 after — exact match). |
| T7.2 | **Approved.** First 2 attempts were false-REJECTs caused by the Codex tool call itself: constructing an inline `node -e "..."` verification one-liner through bash-heredoc→codex→PowerShell triple quoting corrupted the regex, producing `TOTAL_BLOCKS=0` (0 JSON-LD blocks found on a 52-page site — an implausible result that should have been treated as a tooling failure, not evidence). Fixed by writing the verification script to a file and having Codex execute it by path instead of authoring inline shell. Real result: 103 JSON-LD blocks, 0 parse failures, BreadcrumbList=51/52 (every non-home page), ItemList=4 (new), 0 duplicates. Internal reviewer separately APPROVEd with the same independently-run numbers, plus flagged 2 Low items (no direct unit-test assertions for the new context fields; task-description miscounted "5" vs actual "14" brace-fix sites — cosmetic, not a code defect). |
| T7.1 | **Approved.** Audit-only task — Codex independently ran a full-site scan (52 pages) confirming zero duplicate titles/descriptions, zero missing title/description/canonical, all pages exactly 1 `<h1>`, and zero pages falling back to `site.json`'s default SEO strings. Internal reviewer had separately reached the same conclusion first. No code changes made or needed. |
| T6.4 | Round 1: **REJECT** — a real degradation-path bug: `showSuccess()`'s fallback `<p>` (inserted when `[data-success]` is missing from the DOM, per contract §4.5) had no `data-success` attribute, so the `.is-submitted > *:not([data-success])` CSS rule (added this task) would hide it the instant it was appended — the fallback text would never actually be visible. Fixed by giving the fallback `<p>` a `data-success` attribute so it's excluded from the hide rule. Round 2: **Approved**, confirmed the fix resolves the exact failure mode and searched all `[data-success]` usages for new conflicts (none found). Internal reviewer (before Codex) had separately APPROVEd with no findings — this bug was Codex-only, underscoring why the recheck is mandatory even after an internal APPROVE. |
| T6.3 | **Approved.** Internal reviewer first REJECTed for 1 Major: scroll-highlight observed heading elements themselves with a narrow top-30%-viewport `rootMargin` and removed the highlight the instant a heading exited that band, so the TOC showed no highlight for most of actual reading time. Fixed by tracking a single `activeId` that only advances when a new heading enters the band. Codex traced the exact transition sequence through the contract's own example and confirmed correctness. |
| T6.2 | **Approved**, no issues. Internal reviewer also APPROVE; additionally executed the real `tabs.js` against a constructed fake-DOM harness (18 assertions) rather than just reading the code. |
| T6.1 | **Approved.** Internal reviewer found 1 Major: unguarded `{{value}}` in the table-cell template would crash the *entire site build* if any future model lacked a declared column's param. Fixed at the source in `pages.js` (coalesce missing values to `''`, preserving legitimate `0` — NOT a template `{{#if value}}` guard, which would have hidden real zeros). Codex found 1 new Minor (range filter edge case, non-blocking). |
| T5.10 | **Approved**, no new issues. Internal reviewer found and fixed a fabricated `mailto:sales@beiluo.com` (copied from the contract doc's illustrative example, not real data) — replaced with real WhatsApp/WeChat channels. |
| T5.9 | **Approved**, no new issues. Internal reviewer REJECTed for 1 Major dead link (`/about/authors/` breadcrumb crumb pointed to a non-existent page) — fixed. |
| T5.8 | **Approved**, no new issues. Internal reviewer first REJECTed for 2 Major (unstyled card classes, unencoded share URLs) — both fixed. |
| T5.7 | Round 1: 2 High found (missing `.longform` reuse, Related Articles polluting TOC scan scope) → both fixed → Round 2: **Approved**. |
| T5.6 | **Approved**, no new issues. Internal reviewer first REJECTed for 2 Major (missing tag rendering, CSS-hack duplication instead of server-side pre-filter) — both fixed. |
| T5.5 | **Approved**, no issues found. |
| T5.1–T5.4, T4.7 | All approved after fix rounds (High/Med/Low findings resolved) — see `.superpowers/sdd/progress.md` for full per-task detail if needed. |

**Tooling note for future Codex invocations in this repo:** `codex exec -s read-only ...` hangs indefinitely if the prompt is passed as a quoted CLI argument on this Windows/Git-Bash setup (silently waits on stdin, never receives EOF — confirmed via `codex doctor` that network/auth were healthy, so this is an argv-parsing quirk, not an outage). **Fix:** pipe the prompt via heredoc instead: `cat <<'EOF' | codex exec -s read-only -C "<path>" - ... EOF` (the trailing `-` forces stdin read). This works reliably and should be used for all future Codex re-checks in this repo.

**Second tooling note (T7.1):** when a recheck prompt asks Codex to *construct its own* multi-line inline `node -e "..."`/`rg`/`grep -P` command from an open-ended instruction, it drives that command through Windows PowerShell and its own quote-escaping frequently mismatches PowerShell's parser (single/double-quote nesting inside `-Command`), burning 1-2 minutes of retries or timing out entirely (hit once this task). **Fix:** hand Codex a complete, already-correct one-liner command to run verbatim (as done for T7.1's second attempt) rather than describing what to check and letting it author the command itself — it still authors its own verification for anything not explicitly scripted, just skips the fragile self-authored-shell-escaping step for the core data pull.

**Third tooling note (T7.2):** even a pre-written one-liner isn't safe if it contains regex/quotes — Codex still relays it through bash-heredoc→codex-exec→PowerShell, and one of those layers can silently mangle escape sequences, producing a **plausible-looking but wrong result** (T7.2's first attempt returned `TOTAL_BLOCKS=0`, which Codex initially reported as a genuine REJECT rather than recognizing "zero JSON-LD blocks on a 52-page site" as an implausible number worth double-checking before concluding). **Fix:** for anything beyond a trivial command, write the verification script to a file first (`Write`) and have Codex's prompt just say "run `node <path-to-script>`" with no inline quoting at all — this fully sidesteps the multi-layer-escaping problem. Also: when a recheck result looks like "no data found" rather than "data found and it's wrong," treat that as a signal to re-verify the check itself before trusting a REJECT. **Confirmed working in T7.3**: the file-based script approach got a clean first-attempt APPROVE with zero retries — adopt this as the default method going forward, not just a fallback after a failed inline attempt. One more wrinkle to watch for: if the script itself reads another generated file (e.g. a saved build-log), save that file to a plain Windows path too (not bash's `/tmp`, which doesn't reliably resolve from the PowerShell process Codex shells out to).

**Fourth tooling note (T7.4):** `codex exec -s read-only` cannot run `npm test` (or anything spawning child processes) — every test file fails at spawn time with `spawn EPERM`, unrelated to the actual test suite's health. Codex's first instinct was to report this as "npm test unconfirmed" and lean toward REJECT; it only reversed to APPROVE after being shown 3 independent real-environment `npm test` results (363/363 pass each) as evidence that this was a sandbox limitation, not a regression. **Takeaway:** don't ask Codex to run `npm test` (or any command that spawns processes) directly — either skip that check in the recheck prompt and supply your own fresh-run evidence up front, or expect a spurious REJECT and be ready to follow up with a clarification round citing independent real-environment results rather than re-arguing in the abstract.

**Fifth tooling note (T7.5):** distinct from all the above — a genuine transient network failure. 3 consecutive `codex exec` invocations failed with repeated `websocket`/`tls handshake eof` errors before any model response, confirmed as a real (if temporary) connectivity problem via `codex doctor` (`websocket` and `reachability` checks both failed at the time). This is NOT a quoting/sandbox issue — no amount of prompt rewriting fixes it. **Takeaway:** if `codex exec` fails before producing any assistant output (as opposed to failing mid-task or giving a wrong answer), run `codex doctor` to distinguish "Codex is unreachable right now" from "my prompt/script is broken," and simply retry after a short wait — the 4th attempt in this case succeeded cleanly with the identical prompt.

**Sixth tooling note (T8.2):** another sandbox-write limitation, distinct from the earlier `spawn EPERM` (T7.4, blocks process-spawning like `npm test`) — this one blocks plain file writes. Codex successfully deleted `dist/` (`fs.rmSync`) but then `node src/build.js` failed with `EPERM: operation not permitted, open '...\dist\index.html'` when trying to recreate it, i.e. its read-only sandbox permits deleting existing files but not creating/writing new ones in this repo's working directory. **Takeaway:** if a recheck needs Codex to verify something that depends on a *freshly regenerated* `dist/`, don't have Codex do the delete-and-rebuild itself — regenerate `dist/` yourself first (as the controller) and hand Codex a prompt that only reads/audits the existing output, or structure the audit script to read straight from `src/data/*.json` wherever possible so the core assertions don't depend on `dist/` being fresh at all (this is what saved T8.2's recheck — the important checks were source-data-only).

**Seventh tooling note ("Top 8" fix):** a tool-level timeout, not a Codex or sandbox problem. The recheck's Bash-tool invocation of `codex exec` has (like all Bash calls) a fixed timeout (3 minutes unless a longer one is explicitly requested); Codex successfully ran the cleanup + build + audit-script commands and got fully correct output, but the model's final APPROVE/REJECT sentence hadn't been generated yet when the 3-minute mark hit, so the whole call errored out with "Exit code 143 / Command timed out" — an artifact of the *tool call*, not evidence the verification failed. The transcript up to the cutoff is still valid evidence (all command output was already visible). **Takeaway:** if a `codex exec` Bash call times out but the visible transcript shows every verification command already succeeded with correct output, don't treat it as a failed recheck — either re-invoke with an explicit longer `timeout` parameter, or (faster) send a short follow-up `codex exec` that quotes the already-successful output back to Codex and asks only for the verdict, skipping the redundant re-run. Consider requesting a longer timeout (e.g. 180000ms+) up front for any recheck prompt that involves a full build + multi-step audit script, since 3 minutes is tight for that combination.

**T8.1/T8.2 "Tier 1" batch — data/template wiring + title/CTA fixes, no new content required.** User asked to resume T8.1/T8.2's remaining smaller findings and, via `AskUserQuestion`, selected all 4 proposed scope tiers (Tier 1: data/template wiring; Tier 4: `sidebarSections` wiring; Tier 2: new SVG/PDF assets; Tier 3: WeChat popup + Home Appliance solution) rather than picking a subset — this batch covers Tier 1 only, the others are separate future batches.

Fixed: (1) `home.html`'s `hero.bgSvgAlt` now reads the real field value instead of a hardcoded `alt=""` (zero visual change today since the field is empty, but the template now correctly respects it per `home.schema.md`'s own documented intent); (2) `solutionsTeaser[].industry` badge now renders (reusing `.solution-card__industry`, no new CSS); (3) `supportTeaser[].category`/`.date` now render — since the raw `category` field is a slug ("guides") unfit for display, added a `categoryLabel` field to each `home.json` entry matching `support.json`'s human-readable category names, updated `home.schema.md` accordingly; (4) `newsTeaser[].type` re-confirmed **not a defect** (redundant with the already-rendered `categoryTag`); (5) 3 title-keyword gaps fixed in `pages.js` — 4 product-category titles gained `| BeiLuo`, `/support/`'s title gained an "Infineon" prefix, author-page titles had their redundant second "BeiLuo" replaced with "Infineon"; (6) `product-detail.html`'s hero CTA "Request a Quote" → "Get a Quote" (matches design.md §5.4's wireframe exactly); (7) `product-detail.html`'s bottom CTA expanded from 2 to the design-mandated 4 actions (Download Datasheet / Apply for Sample / Get a Quote / Ask FAE) — "Back to Category" dropped since it's not one of the 4 and the page's breadcrumb already covers that nav; (8) `products-list.html`'s category-card link changed from a solid `.btn--cta` to `.btn--secondary` (ghost/outline), matching `solutions-list.html`'s already-correct equivalent; (9) `solutions.json`'s `blockDiagram` object (5 instances, exact duplicate of the actually-rendered `diagramSrc`/`diagramAlt` fields, never read by any template) removed, `solutions.schema.md` updated to document the real fields instead; (10) re-checked the "1 tech-detail metaDescription over 160 chars" finding — all 4 articles are now 141–155 chars, already resolved incidentally by an earlier batch, no action needed.

Verification: `npm test` 365/365 pass; clean rebuild exit 0, zero dead links (including the new `/contact/?ref=...&type=sample`/`&type=fae` query-string links, correctly recognized as pointing to the existing `/contact/` page); full 52-page × 12-width overflow re-sweep zero failures (no regression, including the bottom CTA's 2→4 button expansion at narrow 320-375px widths, confirmed via screenshot to wrap to a vertical stack, not clip). Independent reviewer verified every change against its exact described diff, used real-browser rendering (industry badges, category badges+dates, ghost-vs-solid button styling, CTA wording, all 4 `<title>` tag values) rather than just re-reading templates, and ran its own 52-page overflow sweep — APPROVE, 2 non-blocking Minor (a stale `blockDiagram` mock in `tests/pages.test.js`'s fixture, since fixed to use `diagramSrc`/`diagramAlt`; some harmless JSON reformatting noise in `solutions.json`'s diff from the field-removal script re-serializing the file) + 1 Low (the old `check_list2.md` audit checklist still lists `blockDiagram.src`/`.alt` as required — a stale audit doc, out of this diff's scope, not fixed). Codex approved, confirming the same evidence via its own commands and reading the `pages.js`/`product-detail.html` diffs directly.

---

## 8. Next Recommended Task

**T9.1 and T9.2 are both `completed`.** T8.1 and T8.2's remaining findings are being worked through in user-selected tiers: **Tier 1 (data/template wiring + title/CTA fixes) is done, Codex-approved, and committed (c6cbcae).** User selected all 4 tiers (Tier 1, Tier 4 `sidebarSections` wiring, Tier 2 new SVG/PDF assets, Tier 3 WeChat popup + Home Appliance solution) rather than a subset — proceed through them in that order unless told otherwise.

**Next up: Tier 4 (`sidebarSections`/`solution.related` wiring)** — medium scope, needs a content-structure decision first (what each of the 6 templates using `{{> sidebar}}` should actually show: products-list/product-category probably want "other categories" nav, support-list/tech-detail probably want category+tag nav, solutions-list/solution-detail probably want related-solutions nav). This is genuinely a design decision, not just data plumbing — consider a quick `AskUserQuestion` or explicit confirmation on the intended sidebar content per template before implementing, following this session's established pattern of not silently deciding ambiguous content/design questions.

**Then Tier 2 (new SVG/PDF assets)** — home.json's Why-Choose-Us icons + hero trust badges (~7 SVGs), `src/assets/docs/`'s missing PDFs (tech-detail's "Related PDF Download" links + `category.selectionGuideDownloadHref`), distinct FAE author avatars (currently both share the generic `about-hero.svg`), and more distinct solution/news/support illustrations (currently heavy reuse of a handful of generic SVGs). This is real asset-creation work, likely needing several iterations — scope/style should probably be confirmed with the user before generating anything, given it's visually user-facing and harder to walk back than a text/data fix.

**Then Tier 3 (2 independent bigger tasks)** — the WeChat QR-toggle (needs new JS/CSS/a popup-layout design decision not specified in design.md) and the "Home Appliance" industry gap (needs a genuinely new ~800-word solution, roughly T4.3-scale). Both were already flagged in earlier sessions as warranting their own dedicated task rather than folding into general cleanup.

Note: if `npx playwright install chromium` is ever retried in a future session, it's worth trying again — the CDN geo-block this session hit may not be a permanent restriction, and having Chromium alongside Firefox would let a future browser-dependent task (e.g. verifying new Tier 2 SVG assets render correctly) cross-check rendering in more than one engine.

**When T8.1 is resumed further:**
1. ~~Get a browser check on the new CSS~~ — done via T9.2 (Playwright+Firefox now available in this session); the ~21-page sample looked visually correct and several real bugs it surfaced are already fixed (see §3's T9.2 writeup). A wider sweep of the remaining ~31 untested pages (specific product-detail models, solution/news/support articles) with the same tooling would still be worth doing if convenient, but isn't blocking.
2. Close out the smaller T8.1 findings (§6.B: `sidebarSections`/`solution.related` data wiring — note `sidebarSections` CSS now exists but the data was never wired, so sidebars will render empty, not broken, until this is done; §6.E: missing SVG assets — home.json icons/trust badges, `src/assets/docs/` PDFs; §6.F: title-keyword gaps, CTA text/component mismatches) — individually small, batchable.
3. Re-run the check_list1.md audit for real completion, and only then mark T8.1 `completed` in `todo_write.md`.

**When T8.2 is resumed:**
1. What's left needs real design/content work (not quick data fixes): the WeChat-QR-toggle (needs new JS/CSS/design decisions, its own small task) and the "Home Appliance" industry gap (needs a genuinely new ~800-word solution, its own T4.3-scale task — user explicitly declined to force-fit it into an existing solution).
2. Remaining smaller work is §6.D's placeholder/asset-reuse issues (shared generic illustrations across solutions/news/support, `selectionGuideDownloadHref`'s missing PDFs) — these need new asset creation, not JSON/template edits.
3. Re-run the check_list2.md audit for real completion, and only then mark T8.2 `completed`.

**T9.1 update: the dead-link count is now 0** (was 4, all from home.json's fabricated teaser slugs — see §3's T9.1 writeup). The ~13+ missing SVG/PDF assets (§6.E of both T8.1 and T8.2 findings) are a separate, still-open gap — they don't produce dead *links* (the templates guard optional asset fields with `{{#if}}`), so they don't block T9.1's zero-dead-link criterion, but should still be tracked for completeness.
